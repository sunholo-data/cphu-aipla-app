"use client";

// SCAFFOLD: __PASCAL__LabButton — workspace launcher for the sim.
// Generated by scripts/new-workbench-skill.sh.

interface __PASCAL__LabButtonProps {
  onOpen: () => void;
  disabled?: boolean;
}

export function __PASCAL__LabButton({ onOpen, disabled }: __PASCAL__LabButtonProps) {
  return (
    <button
      type="button"
      onClick={onOpen}
      disabled={disabled}
      className="flex w-full items-center gap-3 rounded-lg border border-border bg-card p-3 text-left text-sm transition hover:border-primary hover:bg-muted/30 disabled:cursor-not-allowed disabled:opacity-50"
      aria-label="Open __TITLE__ simulation"
    >
      <span
        className="flex h-9 w-9 shrink-0 items-center justify-center rounded-md bg-primary/10 text-base font-semibold text-primary"
        aria-hidden="true"
      >
        {/* TODO: 2-letter monogram */}
        ••
      </span>
      <span className="flex-1">
        <span className="block font-medium">Open simulation</span>
        <span className="block text-xs text-muted-foreground">
          {/* TODO: one-line description */}
          Interactive simulation
        </span>
      </span>
      <span className="text-xs text-muted-foreground" aria-hidden="true">›</span>
    </button>
  );
}
