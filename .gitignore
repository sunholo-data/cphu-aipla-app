# Dependencies
node_modules/
frontend/node_modules/
backend/.venv/
cli/.venv/
cli/dist/
cli/build/
__pycache__/
*.pyc

# Build
frontend/.next/
frontend/out/
*.egg-info/
dist/
build/

# Environment
.env
.env.local
.env.*.local
frontend/.env.local
backend/.env

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Coverage
coverage/
htmlcov/
.coverage
.coverage.*
frontend/coverage/
backend/.coverage*

# Firebase
firebase_local/

# SSL certs (local dev)
backend/certs/
*.crt
*.key
*.pem

# Ruff cache
backend/.ruff_cache/

# Pytest cache
.pytest_cache/
**/.pytest_cache/

# TypeScript
tsconfig.tsbuildinfo

# Claude Code local settings
**/.claude/settings.local.json

# Debug
/debug_images/
.claude/scheduled_tasks.lock
backend/aitana_platform/

# Local dev scratch (deployed URLs, ad-hoc dev cache)
.dev-logs/

# Locked agent worktrees from parallel sub-agent execution. The harness
# manages these; they're never committed and shouldn't block sanitize.
.claude/worktrees/

# Terraform local state + provider plugins (never commit — provider binaries are >100MB)
**/.terraform/
*.tfstate
*.tfstate.*
demo-captures/

# Copyrighted literature — private working references (repo is public on
# GitHub). Keep the citation README.md tracked; never the PDFs or their
# full-text parsed extracts. See docs/literature/tp-framework/README.md.
docs/literature/**/*.pdf
docs/literature/**/parsed/
