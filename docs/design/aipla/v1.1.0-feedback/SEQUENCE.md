# v1.1.0-feedback Build Sequence

**Source briefs:** [`june-03-feedback-sprint-brief.md`](file:///Users/mark/Documents/clients/cph-uni/strand-a-pedagogical-bot/prototypes/june-03-feedback-sprint-brief.md) — 3 June 2026 teacher check-in · [`june-09-feedback-sprint-brief.md`](file:///Users/mark/Documents/clients/cph-uni/strand-a-pedagogical-bot/prototypes/june-09-feedback-sprint-brief.md) — 9 June 2026 teacher check-in (rows 1.1.20–1.1.25)
**Target window:** post-v1.0 / pilot-iteration (2026-06-04 → pilot start 2026-08-14, with continued iteration through 2026-09-15 handover)
**Parent roadmap:** [../SEQUENCE.md](../SEQUENCE.md)

This is the per-version sequence file for the **v1.1.0-feedback** batch. It began as nine items distilled from the 3 June teacher check-in (1.1.1–1.1.9), grew with voice/chat-polish/security follow-ups (1.1.10–1.1.19), and absorbed the **9 June teacher check-in** as rows **1.1.20–1.1.25** (see the *9 June teacher check-in batch* sub-table). The briefs are the source of pedagogical / product truth; the docs here add the repo-specific technical execution layer (file paths, wire shapes, ADR alignment, acceptance gates).

> **9 June headline:** the one date-forced item is **1.1.23 bidirectional voice (target 2026-06-23)**. Everything else in the 9-June batch is either a small Part-1 extension (1.1.20–1.1.22) or a post-mid-point Part-2 design doc gated on existing infra (1.1.24 on 1.J, 1.1.25 on 1.3).

> **v1.1, not v1.0.** Brief tags these as v1.1 priorities; they refine v1.0 surfaces rather than ship new strands. Many can land before the 2026-08-14 pilot, which is good — every item the pilot sees with confidence is one less thing the post-pilot iteration window has to absorb.

## Ordering

Brief priority is preserved; cross-cutting / blocking-dep ordering is noted in the dependency column.

> **Per-row build status (verified 2026-06-08): see [Build status](#build-status--verified-2026-06-08) below.** The inline "shipped" notes in the table rows are partial/stale; the Build status section is the authoritative source of done-vs-open, verified against code rather than doc placement.

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.1 | [tutor-verbosity-fix.md](implemented/tutor-verbosity-fix.md) — sprint: [quick-wins-v1.1-sprint.md](implemented/quick-wins-v1.1-sprint.md) (Track A) | **P0 IMMEDIATE** | ~2h | None | System-prompt-only delta across Boldkast, LED Planck, KineBot SKILL.md preambles. ≤3 sentences per turn unless explicitly asked; end every turn with a question. AR sign-off on the rewritten prompt block at PR review |
| 1.1.2 | [proactive-sim-reactive-tutor.md](implemented/proactive-sim-reactive-tutor.md) — sprint: [proactive-sim-reactive-tutor-sprint.md](implemented/proactive-sim-reactive-tutor-sprint.md) | **P1** | ~1d | [proactive-tutor.md](../v1.0.0-pilot/proactive-tutor.md) Phase A shipped; workbench-event stream from MCPAPP-SPEC | **Promotes / reshapes** the existing proactive-tutor Phase B (idle heartbeat → sim-event-reactive). Trigger on meaningful workbench events (sim run, step change, measurement commit) not slider drag. 90s cooldown, max 2 per session, short observation + question. Per-skill config. **Architecture (sprint plan, Path B):** FE-initiated AG-UI run via synthetic sentinel — backend owns the gate decision endpoint; FE kicks off the actual agent run via the existing `/api/chat/{skill_id}` AG-UI endpoint so the proactive turn rides the established protocol. Phase A's `/greet` REST shape becomes a follow-up refactor candidate after this lands |
| 1.1.3 | [student-consent-prompt.md](student-consent-prompt.md) | P1 | 0.5d FE + 0.5d BE | **JB sign-off on wording** (same approval gate as [audio-capture-and-tts.md](../v1.0.0-pilot/audio-capture-and-tts.md)) | One-shot opt-in at session start. `consent_given: bool` on the session Firestore doc; declines → chat turns NOT written to BigQuery; teacher report shows `No research consent` badge. No change to ADR-001 anonymous-group auth |
| 1.1.4 | [session-report-summary-primary.md](session-report-summary-primary.md) | P1 | ~1d | None (teacher-insights-dashboard shipped) | Flip the session-report layout: AI summary primary; full transcript collapsed by default; CSV download for researcher use. Summary-generation prompt updated to produce 3-5 sentence narrative + concept/parameter/checklist bullets + "what next" line. Also the privacy strategy for eventual audio inclusion (summary has lower privacy profile than verbatim) |
| 1.1.5 | [researcher-role.md](researcher-role.md) | P1 | ~1d | [teacher-permission-model.md](../v1.0.0-pilot/implemented/teacher-permission-model.md) (1.A shipped) | New `role:researcher` Firebase custom claim; bypasses class-tag namespace; `--all-classes` flag on `aiplatform logs`; "Research view" toggle in teacher UI. Set manually by admin for JB, AR, M |
| 1.1.6 | [group-code-school-year-ttl.md](implemented/group-code-school-year-ttl.md) — sprint: [quick-wins-v1.1-sprint.md](implemented/quick-wins-v1.1-sprint.md) (Track B) | P1 | ~2h | [session-persistence.md](../v1.0.0-pilot/implemented/session-persistence.md) (1.F shipped) | **Rescoped 2026-06-03 mid-sprint.** Original "extend default to 300d" reshaped on privacy grounds (longer default = larger PII / consent surface on every code). This row now ships **rename** (`DEFAULT_TTL_DAYS` → `DEFAULT_GROUP_CODE_TTL_DAYS`, value stays 30) + **soft archival on expiry** (graceful 410 Gone; BQ rows retained). The per-code TTL choice moves to 1.1.10 below |
| 1.1.10 | [teacher-choice-ttl.md](teacher-choice-ttl.md) | P1 | ~0.5-1d | 1.1.6 (rename shipped); [teacher-permission-model.md](../v1.0.0-pilot/implemented/teacher-permission-model.md) (1.A shipped) | **New 2026-06-03** — split from 1.1.6. Teacher picks per-code TTL via preset dropdown (30/90/180/300d + custom 7-365d range) at group-code creation. Backend `mint_group(ttl_days=...)` already supports this; what's missing is route validation + teacher-UI form field + class-detail display + CLI `--ttl-days` parity. Preserves the brief's school-year ask as an opt-in per code rather than a platform-wide default |
| 1.1.7 | [student-multimodal-upload.md](student-multimodal-upload.md) | P1 | ~2d | ADR-008 (Gemini multimodal via AILANG Parse — ready); JB confirm on image-retention posture | **Was originally planned as 1.10** `multimodal-ingestion-via-ailang-parse.md` in the parent SEQUENCE — this doc supersedes that planned row. Most-requested student-facing item from 3 June. Paperclip upload, JPG/PNG/HEIC/PDF, image thumbnail in chat, Gemini multimodal call with image attached. Tutor handles handwritten diagrams, experimental-setup photos, draft answers per skill prompt |
| 1.1.8 | [exit-ticket.md](exit-ticket.md) | P1 | ~1d | **JB/AR providing the question set** | End-of-session modal with confidence emoji rating + free-text. Stored in Firestore session doc + BigQuery (`exit_ticket_rating`, `exit_ticket_text`, `exit_ticket_skipped`). Teacher session report shows the rating; researcher view aggregates |
| 1.1.9 | [cost-dashboard.md](cost-dashboard.md) | P1 | ~1d | [chat-log-pipeline.md](../v1.0.0-pilot/implemented/chat-log-pipeline.md) (1.2 shipped — token counts already in BQ via OTel) | **Supersedes the originally-planned 1.12** `budget-dashboard.md`. Teacher class-detail "Budget" panel: this-month spend, per-activity / per-group breakdown, projected monthly. Researcher cross-class spend. Priority lifted by DK's Indian cohort scaling to ~100s of students |
| 1.1.11 | [voice-provider-abstraction.md](implemented/voice-provider-abstraction.md) — sprint: [voice-provider-abstraction-sprint.md](implemented/voice-provider-abstraction-sprint.md) | P1 | ~3.5-4d (Phase A ~1.75d + Phase B ~1.5-2d) | [audio-capture-and-tts.md](../v1.0.0-pilot/audio-capture-and-tts.md) (Part 1 shipped); 1.1.9 cost-dashboard for span surfacing; 1.G teacher UI for per-class toggle; 1.1.2 proactive-sim-reactive (auto-read toggle picks up proactive turns automatically) | **New 2026-06-03** — voice quality flagged today (choppy macOS Danish "Sara") + dictation requested as "lesson ease" accessibility upgrade. Server-side `TTSProvider`/`STTProvider` Protocols in `backend/voice/`, Cloud TTS (Standard spike → WaveNet upgrade) for Danish read-aloud + Cloud STT `latest_long` for Danish dictation, browser-native stays as default provider. Per-class teacher opt-in for the mic button. **Auto-read toggle**: student-side preference to auto-speak every assistant message (incl. proactive turns from 1.1.2) vs current click-to-read default. Designed swap-shaped (cloud → server-local → self-hosted Whisper on UCPH GPU per ADR-003 four-tier model). Defers Gemini Live / LiveRunner to a future conversational-tutor doc. **Phase A (TTS) ships independently** — fixes today's Sara issue without waiting for Phase B |
| 1.1.12 | [voice-personas.md](voice-personas.md) | P2 | ~3d | [voice-provider-abstraction.md](implemented/voice-provider-abstraction.md) (1.1.11 shipped 2026-06-04); 1.G teacher class-detail page for picker placement; optional 1.1.9 cost-dashboard for per-persona spend split; optional 1.1.5 researcher-role for `?debug=voice` without query param | **New 2026-06-04** — UX polish over 1.1.11. Teacher review found the technical voice picker ("gcp_chirp3hd / da-DK-Chirp3-HD-Aoede") opaque to non-engineers. New `Persona` model bundles avatar + name + title + language + voice; teachers pick a persona card instead of tier+voice dropdowns. Chat bubble shows persona avatar + name; new friendly status pill replaces the technical debug pill (technical mode behind `?debug=voice`). 4-6 default personas (Aoede/Charon/Kore/Puck/Frida/Daniel) ship as YAML in `backend/personas/`. Existing 1.1.11 raw voice config stays as "Custom voice (advanced)" expander. Net +7 axiom score; +1 USABLE BY DESIGN explicit win |
| 1.1.13 | [implemented/sim-onboarding-ergonomics.md](implemented/sim-onboarding-ergonomics.md) — sprint: [implemented/sim-onboarding-ergonomics-sprint.md](implemented/sim-onboarding-ergonomics-sprint.md) **shipped 2026-06-04** | **P1** | ~0.75d | [implemented/proactive-sim-reactive-tutor-sprint.md](implemented/proactive-sim-reactive-tutor-sprint.md) shipped | **Shipped 2026-06-04** — follow-up to 1.1.2. PROACTIVE-SIM-REACTIVE shipped over several iteration loops because per-artefact frames used an **allowlist** filter ("explicitly route each kind") which silently dropped multi-word hyphenated kinds (`kinebot.sim-run`, `led-planck.auto-run`) — proactive turns silently never fired. Inverts to **denylist** via new `useArtefactReportEvent` hook (drop noisy kinds, forward everything else with default-through `{kind}` shape). Refactored Boldkast / LED Planck / KineBot frames. 23 new vitest cases (8 unit + 7+10+6 integration) pin the regression bar. Hardened `_sim-template/` scaffold + cross-linked `mcp-app-artefact` skill so future sims (Pendul, Kredsløb, Videoanalyse, GPS Fart, Frekvensanalysator) onboard mechanically |
| 1.1.14 | [implemented/chat-history-flicker-on-token-refresh.md](implemented/chat-history-flicker-on-token-refresh.md) — sprint: [implemented/chat-history-flicker-on-token-refresh-sprint.md](implemented/chat-history-flicker-on-token-refresh-sprint.md) **shipped 2026-06-04** | **P1** | ~2-3h | [session-persistence.md](../v1.0.0-pilot/implemented/session-persistence.md) (1.F shipped) + Vertex Agent Engine session backend (Decision 13 in [bootstrap-aipla-dev.NOTES.md](../../../../scripts/bootstrap-aipla-dev.NOTES.md)) | **Shipped 2026-06-04** — student-reported chat-bubble flicker (history disappears for ~400 ms then reappears) traced to `AGUIProvider` unmounting its subtree on Firebase ID-token refresh. Bug exists in the inherited template but is masked there by sub-50 ms in-region Agent Engine reads; surfaced on AIPLA by the cross-region (europe-north1 ↔ europe-west1) Vertex hop. Frontend-only fix: `hadTokenOnceRef` + gate `tokenResolved` blanking on **initial** load only, not on every `user`-ref change. 11 LOC impl + 3 new vitest cases in AGUIProvider + 1 defensive vitest in useSessionMessages. Filed as [upstream-feedback.md entry #31](../../../upstream-feedback.md) for the template |
| 1.1.15 | [implemented/chat-svg-streaming-placeholder.md](implemented/chat-svg-streaming-placeholder.md) — sprint: [implemented/chat-svg-streaming-placeholder-sprint.md](implemented/chat-svg-streaming-placeholder-sprint.md) **shipped 2026-06-04** | P2 | ~1.5h | None | **Shipped 2026-06-04** — student-reported SVG diagram flicker as the closing `</svg>` token arrives. Cause: `ChatMarkdown`'s pre-processor only matches a complete `<svg…</svg>`, so the partial is stripped via the `html()` handler until the closing tag arrives — at which point a 160 px placeholder appears suddenly (vertical jump), then the SVG fills it after DOMPurify resolves. Fix: added a tail-regex (`SVG_STREAMING_TAIL_RE`) that detects an open `<svg` without a close, reserves the same 160 px placeholder immediately. When `</svg>` lands, React reconciles the placeholder in-place with the rendered SVG — no second jump. 25 LOC impl + 5 new vitest cases (~100 LOC) in `ChatMarkdown.test.tsx`. Shared `SvgStreamingPlaceholder` export from `SVGBlock` keeps dimensions identical across the streaming/post-sanitise states |
| 1.1.16 | [security-monitoring-pipeline.md](implemented/security-monitoring-pipeline.md) — sprint: [implemented/security-monitoring-pipeline-sprint.md](implemented/security-monitoring-pipeline-sprint.md) **shipped 2026-06-05** | P1 | ~6h (M1+M2+M5 critical path, M3+M4 layered same-day) | None — additive on existing `ci.yml` | **Shipped 2026-06-05** — Prompted by the 20-alert dependabot pile (commits `084920b` + `1257c08` patched all 20). Three-layer pipeline now live: (1) `security-audit` job in [ci.yml](../../../../.github/workflows/ci.yml) runs `scripts/security-check.sh` (the single source of truth for the gate); fails PRs on new HIGH/CRITICAL production-dep CVEs. (2) [security-weekly.yml](../../../../.github/workflows/security-weekly.yml) cron Mon 09:00 UTC updates a rolling tracking issue with audit + dependabot summary. (3) [aipla-security-checkup skill](../../../../.claude/skills/aipla-security-checkup/SKILL.md) encodes the reachability rubric. Side effect of the sprint: closed 13 newly-discovered backend Python CVEs (litellm bump to 1.85.4, aiohttp/idna/mako/pyjwt/python-multipart/urllib3/authlib bumps via `uv lock --upgrade-package`) + 3 frontend high CVEs (undici→7, fast-uri→3.1.2, fast-xml-builder→1.2.0 via overrides). 1 documented ignore (starlette PYSEC-2026-161, blocked by fastapi<1.0.0). Frontend 911 tests + backend 1761 tests pass. Net axiom score +4 (SECURE BY CONSTRUCTION, EARNED TRUST, OBSERVABLE BY DEFAULT, PROTOCOL OVER CUSTOM all +1) |
| 1.1.17 | [student-engagement-signals.md](student-engagement-signals.md) | **P2** | ~1.5d | [chat-log-pipeline.md](../v1.0.0-pilot/implemented/chat-log-pipeline.md) (1.2 shipped); 1.1.3 consent; 1.1.4 session-report surface; 1.1.5 researcher cohort view; JB/AR review of metric set + framing copy | **New 2026-06-06** — formative-not-sanctionary engagement signals on the teacher session report (paste ratio, revision count, turns-to-completion, abandonment point, re-ask rate). Framing is explicit: cohort signals → "rewrite problem N", not per-student sanction. Origin: 2026-06-05 chat with M re: anti-cheat — agreed *if the student is trying to cheat the battle is already lost*; instead these signals surface where the lesson needs improvement. Captures composition counts (not the keystream) FE-side, re-uses existing embedding for re-ask scoring BE-side, extends `chat_turns` BQ table with 4 columns + adds session-level aggregates to Firestore. Researcher view (per 1.1.5) gets a per-class "Engagement" tab with per-problem heatmap — the headline view for turning signals into actionable rewrites. Sits thematically near 1.1.4 (session report) and 1.1.9 (cost dashboard) on the teacher dashboard improvements axis |
| 1.1.19 | [teacher-activity-authoring.md](teacher-activity-authoring.md) | **P1** | ~6–8d full / ~1.5d M0 thin slice | [teacher-ui.md](../v1.0.0-pilot/implemented/teacher-ui.md) (Ph2 ActivityConfig shipped); [expanded-workbench-types.md](../v1.0.0-pilot/expanded-workbench-types.md) (1.J); **JB/AR on quiz format (M2) + first workbench type (M4)** | **New 2026-06-08** — the umbrella for *"teacher creates activities from scratch, non-sim first class"* — recorded as **the primary design priority in the 3 June check-in** (scoping site `strands.qmd`). Unifies the three authoring tiers (tier-1 forms shipped; tier-2 params + tier-3 code are the post-pilot stubs) and fills two gaps: teacher-authorable **non-sim activities** (quiz/notebook/drawing/none) via **A2UI**, and a **teacher-authored declarative MCQ** quiz (distinct from KineBot's tutor-driven adaptive quiz). MCP Apps stay the rail for complex sims (ADR-013). Extends `ActivityConfig` with `workbench_type`/`checklist`/`quiz`/`materials`; generalizes the shipped `ProgressChecklist`. **9 June adds M6 (equipment co-design) + the curriculum-library `materials` picker (split to 1.1.25).** **Aggressive v1.1 target, phased** — M0 (no-workbench concept activity, ~1.5d) lands pre-freeze and is independently valuable; M1–M6 are individually shippable through pilot iteration. Realism: largest open item, competes with the other P1s + freeze — phasing is the descope discipline |
| 1.1.27 | [lesson-author-surface.md](lesson-author-surface.md) | **P1** | ~2d (~1.75d eng + buffer) | [1.G teacher-ui](../v1.0.0-pilot/implemented/teacher-ui.md) + [1.1.12/1.1.20 personas+style](tutor-personas.md) + [1.1.25 curriculum](curriculum-library.md) all SHIPPED (their outputs are the prompt sources the preview attributes); **JB sign-off** on what the preview reveals | **Re-scoped 2026-06-12** (renumbered 1.1.17→1.1.27; original 1.1.17 went to `student-engagement-signals`). Drafted 2026-06-05 with four capabilities; **three have since shipped under their own rows** and are removed/cross-referenced — bounded knobs → **1.1.20** `interaction_style`; curriculum upload → **1.1.25** curriculum-library; chat-only `concept-dialogue` skill → **1.1.19** `none`/chat activity type. **This row is now just the two net-new, uncovered pieces:** (1) **resolved-prompt preview** — read-only, source-attributed view of the system prompt (now *more* valuable: persona/style/curriculum are all hidden contributors); (2) **trial session** — "Try this lesson", teacher-as-student, `is_trial=true`, excluded from class analytics + research aggregation. Backend work is mostly *factoring existing composition into one `assemble_prompt()` with a provenance map*, not new logic. Net axiom score **+6**. Parent SEQUENCE 1.9 (chat-only Socratic) now folds into **1.1.19**, not this row |

### 9 June teacher check-in batch (added 2026-06-09)

Source: [`june-09-feedback-sprint-brief.md`](file:///Users/mark/Documents/clients/cph-uni/strand-a-pedagogical-bot/prototypes/june-09-feedback-sprint-brief.md) + [`notes/2026-06-09-curriculum-content-uses.md`](file:///Users/mark/Documents/clients/cph-uni/notes/2026-06-09-curriculum-content-uses.md). Part 1 (§1–3) extends shipped/in-flight work; Part 2 (§A–C) are new design docs; Part 2 §D (friction/timing analytics) is **R1-gated** (analytics-framework decision due before the 29 June freeze) and is **not** a new app row — it folds into [`teacher-analytics-framework`](../post-pilot/session-analytics-rubric.md) / 2.5 once R1 lands. **The one urgent new date is 1.1.23 (bidirectional voice, 2026-06-23).**

> **Prioritisation lens — breadth over depth (9 June meeting; memory `aipla-breadth-over-depth`).** Year 1 favours **coverage of the interface possibility-space over depth/polish on any single feature** — many thin probes, not a few deep builds. Read the batch through it: 1.1.20 personas, 1.1.22 notes-summary, 1.1.23 voice modes, 1.1.24 offline-lab are each **new-interface probes** (good for breadth); **1.1.25 curriculum-library is the breadth *multiplier*** — it drops the marginal cost of every new activity (grounding + rubric + level-calibration for free). Two curriculum uses are **pulled forward** because they *operationalise* the strategy: **auto-drafted rubrics (#4)** keep each probe measurable, and the **coverage/gap map (#6)** says where to probe next (these live in 1.1.19 M7/M8, sourced from 1.1.25).

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.20 | [tutor-personas.md](tutor-personas.md) | **P1** | ~1d | [tutor-verbosity-fix.md](implemented/tutor-verbosity-fix.md) (1.1.1 — Socratic preset generalized); **AR sign-off per preset prompt**; rides 1.1.19 activity-config surface | **New 2026-06-09 (§3)** — per-activity teaching-style presets (Socratic default / concise-directive / rigorous "hardcore" / warm). **Resolves the apparent 1.1.1-vs-9-June conflict**: "end every turn with a question" is the *Socratic preset*, not a global law. Field `interaction_style` — deliberately named to avoid the 1.1.12 `persona` (voice/avatar) collision; the two are orthogonal axes. Gated only on AR prompt sign-off |
| 1.1.21 | folded into [student-multimodal-upload.md](student-multimodal-upload.md) (1.1.7) | **P1** | ~1d + ~2h | **1.1.7 must land first**; **M (GDPR)** on guardrail detection approach (on-device vs Gemini-vision pre-check) | **New 2026-06-09 (§1)** — no-person-in-frame guardrail (pre-upload notice + person/face block-and-retake) + units loop ("what are the units?" before other feedback). Keeps uploads' consent profile low (concern is *only* a person in frame). Extends the 1.1.7 doc rather than a new doc; tracked here for sequencing |
| 1.1.22 | [end-of-class-notes-summary.md](end-of-class-notes-summary.md) | **P1** | ~1d (on top of 1.1.7 + 1.1.8) | **1.1.7 (upload) + 1.1.8 (exit ticket)** + 1.K DRA goals; JB note-retention (reuse 1.1.7) | **New 2026-06-09 (§2)** — no-laptop reality: student photographs handwritten notes at class end → AI summary measured against the activity's learning goals (captured / missing / one revisit). Composes 1.1.7 path + 1.1.8 surface; formative, non-retained, shared-phone |
| 1.1.23 | [bidirectional-voice-brief.md](bidirectional-voice-brief.md) | **P1 — URGENT (target 2026-06-23)** | axis ~0.5d + per mode: `stt_tts_roundtrip` ~0.5–1.5d, `gemini_live` ~3–5d | **1.1.11 voice-provider-abstraction (shipped)**; **M per-mode GDPR + JB first-enabled mode — resolve immediately**; ADR-003/005/007 | **New 2026-06-09 (§C) — the one urgent new date.** Sound in AND out as a **swap-shaped `voice_mode` config axis** (same pattern as ADR-003 model tiers): **`gemini_live` (duplex/LiveRunner) and `stt_tts_roundtrip` (turn-based over shipped 1.1.11) are coexisting mode *options*, not a pick-one build.** Ships the axis + ≥1 mode for the date (`stt_tts_roundtrip` certain; `gemini_live` enabled by config when LiveRunner + audio review land). GDPR gate lives **on the mode** (`gemini_live` = full audio review), not the axis |
| 1.1.24 | [offline-lab-workbench.md](offline-lab-workbench.md) | **P1** | ~3–4d | [expanded-workbench-types.md](../v1.0.0-pilot/expanded-workbench-types.md) (1.J Type 5 lab-notebook); 1.1.19 (`notebook` activity); [curriculum-library.md](curriculum-library.md) (procedure PDFs); **JB/AR ground-truth model + 2 real experiments** | **New 2026-06-09 (§B)** — Haka/matematikfysik labs are offline PDFs: teacher sets up the experiment, students run it physically + enter readings, AI checks entered data for mistakes in chat. **Crux: teacher-supplied ground truth (ranges/relationships); the AI never invents expected values** (anti-hallucination eval = ship gate). Deterministic-check-first, model-explains-second. Post-mid-point build |
| 1.1.26 | [teacher-ui-consolidation.md](teacher-ui-consolidation.md) | **P1 — enabler** | ~5–7d phased (P1 primitives ~1.5d first) | None (frontend-only; no API change) | **New 2026-06-09 (M)** — the teacher surface is a *mess of configs* accreted page-by-page (716-LOC class page, 3 parallel analytics surfaces, half-stub builder tabs, no nav); make it as simple/polished as the student UI. Ships a **teacher design system + IA + progressive-disclosure config pattern** so each new config is a one-line `SettingRow`, not a bespoke panel. **A breadth multiplier on the teacher side** — lowers the cost of every future teacher config. **P1 (primitives) should precede the config-heavy 9-June rows** (personas/voice/TTL/budget/quiz) so they land clean, not into the mess + refactor twice |
| 1.1.25 | [curriculum-library.md](curriculum-library.md) | **P1 — breadth multiplier** | ~3–4d (**ADK RAG — no vector-DB build**) | **ADK RAG (managed) — un-gated from pgvector (2026-06-09)**; ADR-004 (AILANG Parse) + ADR-010; **JB/M copyright clearance** for shared corpus; B/C corpus parsed (base note) | **New 2026-06-09 (§A library half + `curriculum-content-uses` note)** — referenceable A/B/C corpus (uvm.dk + uploads). **One store, 11 uses** (citation, currency, rubrics #4, level-cal #5, coverage-map #6, exam-format #7, equipment-match #8, misconception #9, terminology #10, eval domain #11). **The breadth multiplier**: drops the marginal cost of every new activity. **Retrieval backend swap-shaped: ADK RAG now, pgvector/local as Year-2 migration** — so this is *no longer blocked on 1.3* and can land much earlier/cheaper. #4/#5/#6 logic lives in 1.1.19 (M7/M8); this is their source. Split out of 1.1.19; co-design stays 1.1.19 M6. ACL deny-by-default, copyright-gated. 2010 exam archive stays **out** (Strand C) |

### 15 June teacher check-in batch (added 2026-06-15)

Source: [`notes/2026-06-15-teacher-feedback.md`](file:///Users/mark/Documents/clients/cph-uni/notes/2026-06-15-teacher-feedback.md) + the repo-side map [june-15-feedback.md](june-15-feedback.md). **15 June was mostly downstream of the 9 June framing — most of it *amends* docs already on the roadmap rather than opening new ones.** Two genuinely-new build rows (1.1.29 call-teacher, 1.1.30 mobile-perf) + one R1-gated doc (1.1.31 live teacher dashboard); the rest folds into in-flight docs or is already shipped. Lands ~10 days before the mid-point review (26 June), with a **demo on 16 June** and the **23 June voice** target still the one hard pre-freeze date. **No scope explosion — on track.**

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.29 | [call-teacher.md](call-teacher.md) | **P1 (build-ready)** | ~1d (minimal raised-hand: M0+M1) | None for the minimal slice; M2 folds into 1.1.31 | **New 2026-06-15** — student escalates to a human mid-session. Student chat-composer control → **group-scoped** "raised hand" signal (per ADR-001, never per-student) → teacher live view. Minimal version rides the existing 30s class poll (extend `recent-sessions` payload) + a ~10s `/api/classes/{id}/signals` poll; new `group_signals/{group_id}` doc + `raise-hand`/`ack` endpoints. **Demo-friendly, no human gate.** Full surface integration folds into 1.1.31. Net axiom +9 |
| 1.1.30 | [mobile-performance-pass.md](mobile-performance-pass.md) | **P1** | ~1d profile + per-bottleneck fix proposal | None to start; **report findings before fixes** | **New 2026-06-15** — runtime perf on the single shared low-mid Android phone (distinct from the mobile-*layout* already shipped). **Investigation → targeted fixes.** No perf tooling exists today (no bundle-analyzer, no Lighthouse, no web-vitals; workbench sims load upfront, not code-split). Phase 0 = a profiling note (top-3 bottlenecks, measured, ranked + a fix each); Phase 1 fixes scoped after. Coordinates the **voice latency budget** measurement (1.1.23) on the same device. Net axiom +5 |
| 1.1.31 | [teacher-analytics-framework.md](teacher-analytics-framework.md) | **P1 — R1-GATED** | shell+calls+deterministic ~1.5–2d (un-gated); LLM summary +1–2d (post-R1) | **R1 framework decision** gates the rubric/summary layer (due before the 2026-06-29 freeze); 1.1.29 (raised-hand surface); 1.1.4 (`reports/narrative.py`); 1.2 (BQ) | **New 2026-06-15** — the **live, in-lesson** teacher dashboard: a rolling **~5-min class summary** (wanted/push, not a stressor [M, 15 June]) that hosts the 1.1.29 raised-hand calls. **Distinct from the *post-hoc* [2.5 session-analytics-rubric](../post-pilot/session-analytics-rubric.md)** (which scopes out real-time). **Split build:** the shell + raised-hand panel + deterministic signals (active/idle, turns, "stuck") are **un-gated**; the LLM/rubric summary layer is **R1-gated — do not instrument before R1.** Net axiom +8 |

**Amendments (not new rows) from 15 June — applied in place:**

- **1.1.19 [teacher-activity-authoring](teacher-activity-authoring.md)** — "teacher chooses RAG inputs" added as a first-class authoring control. **The scoping already ships** (1.1.25 M3 `build_curriculum_retrieval_tool(materials)` scopes `VertexAiRagRetrieval` to cited `rag_file_ids`, deny-by-default); 15 June wants the source set *visible + editable* in the builder, folding into the Materials picker — not a new row.
- **1.1.23 [bidirectional-voice-brief](bidirectional-voice-brief.md)** — explicit **latency budget** (p50/p95 round-trip) added as an acceptance criterion; the STT+TTS-vs-`gemini_live` choice is now partly on latency grounds. Mobile is the worst case → shares 1.1.30's measurement.

**Already shipped / not an app task (15 June):** researcher-role = 1.1.5 SHIPPED (ADR-016 closes it in architecture); student upload = 1.1.7 SHIPPED, teacher upload = 1.1.25 SHIPPED (the "where is upload" chase); scenario-didaktik bot = **non-AIPLA spin-off** (track only); "most teachers use AI via CoPilot/school IT" = JB strategy; "weaker students use AI worse, active learning lifts some" = pilot research observation. See [june-15-feedback.md](june-15-feedback.md) for the full disposition map.

### 17 June teacher check-in batch (added 2026-06-17)

Source: [june-17-feedback.md](june-17-feedback.md) (17 June note — Aswin). Two new rows that **deepen the teacher-authoring axis** ([1.1.19](teacher-activity-authoring.md)): a richer set of teacher-authorable workbench elements, and an AI co-pilot that assembles them for non-technical teachers. Both are **breadth probes on the authoring surface** (memory `aipla-breadth-over-depth`): 1.1.38 lowers the marginal cost of every future element; 1.1.39 is a net-new authoring *interface*. Neither is date-forced; 1.1.39 M0 is gated on Aswin's incoming meta-prompt. The 17-June note's other items (RUBUS GPU cluster / IT-resource list, teacher-created video-coded simulations) are **not** app rows here — they track to the [self-hosting/IT-resources handover](../v2.0.0-handover/self-hosting-and-terraform-handover.md) (3.2) and the tier-3 [teacher-artefact-authoring](../post-pilot/teacher-artefact-authoring.md) (2.4) threads respectively.

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.38 | [activity-elements-palette.md](activity-elements-palette.md) | **P1** | ~1d M0 substrate + ~1–2d per element | [teacher-activity-authoring.md](teacher-activity-authoring.md) (1.1.19 — parent builder); [expanded-workbench-types.md](../v1.0.0-pilot/expanded-workbench-types.md) (1.J); [offline-lab-workbench.md](offline-lab-workbench.md) (1.1.24 — table shares wire); [documents-workbench-surface.md](documents-workbench-surface.md) (1.1.33 — document element reuse); **JB/AR on which elements real lessons need** | **New 2026-06-17** — generalise the activity **element** layer from 3 hardcoded fields (checklist shipped, quiz designed, materials shipped) into a **bounded element registry**, so adding the next element is a ~1d recipe not a schema rewrite. Adds four elements: **data table, chart, calculator (formula — the Axiom-9 surface, safe-grammar eval), inline document**. The breadth multiplier on the element axis. M0 (registry) is the leverage; each element is an independent JB/AR-pickable probe. Calculator gated on safe-eval lib choice + formula set |
| 1.1.39 | [activity-authoring-assistant.md](activity-authoring-assistant.md) | **P1** | ~1.5d M0 + ~1d/milestone | [teacher-activity-authoring.md](teacher-activity-authoring.md) (1.1.19 — re-homes its M6/M7/M8 as tools); [activity-elements-palette.md](activity-elements-palette.md) (1.1.38 — `add_element` source); [curriculum-library.md](curriculum-library.md) (1.1.25 — grounding); [lesson-author-surface.md](lesson-author-surface.md) (1.1.27 — verification handoff); **Aswin's meta-prompt** (gates M0); ADR-001 (**teacher auth, not group**), ADR-003 | **New 2026-06-17 (Aswin)** — an in-builder AI **chat co-pilot** that helps a non-technical teacher write a good Socratic **lesson prompt** *and* assemble the right **elements**. A teacher-facing ADK agent whose tools (`set_lesson_prompt`, `propose_checklist`, `add_element`, `suggest_materials`, …) write **proposals** into the `ActivityConfig` draft (human accept/edit, never auto-publish). **Unifies** 1.1.19's headless M6 (equipment co-design) + M7 (auto-rubric) + M8 (coverage) as conversational tools. **⚠ teacher-auth surface** (the recurring 401 corner — `useTeacherAuth`). M0 = prompt co-pilot, gated on Aswin's meta-prompt; degrades to the manual builder when absent |

### 22–23 June batch — activity-builder polish + sharing (added 2026-06-23, M)

Source: M's live review of the shipped activity builder (2026-06-22) + the **23 June ask** for cross-class / cross-teacher activity reuse (*teachers expect "class → pick activities", their own and others'*). These deepen the **teacher-authoring axis** ([1.1.19](teacher-activity-authoring.md)) past the [element palette](activity-elements-palette.md) (1.1.38): a live **preview**, **sims as reusable resources**, a sim-catalogue **CMS**, and the **activity-library + sharing** model that lets one activity reach many classes and teachers. 1.1.40–1.1.41 are pre-pilot (shipped); 1.1.42 is post-pilot; **1.1.43 was pulled pre-pilot on 2026-06-24** (sharing-scope gate resolved — research-mode trusted-set sharing *is* pilot behaviour). The **Build status** table below is the verified done-vs-open source.

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.40 | [activity-preview-mode.md](activity-preview-mode.md) | **P1** | ~1–1.5d | [1.1.38](activity-elements-palette.md) (elements); [1.1.41](teacher-sim-resources.md) (sim render) | **New 2026-06-22 (M)** — live in-builder workspace preview, exactly as a student sees it. M0 shipped 2026-06-22; M1 edit-page parity + M2 side-by-side + full-screen pop-out shipped 2026-06-23 via the shared `StudentWorkspace`/`ActivityBuilderBody` |
| 1.1.41 | [teacher-sim-resources.md](teacher-sim-resources.md) | **P1** | ~2d | [1.1.38](activity-elements-palette.md); ADR-013 | **New 2026-06-22 (M)** — sims as reusable activity resources (artefact catalogue decoupled from the activity). M0–M3 shipped pre-pilot; M4 legacy migration post-pilot |
| 1.1.42 | [sim-catalogue-admin.md](sim-catalogue-admin.md) | **P2 — POST-PILOT** | ~3–4d | [1.1.41](teacher-sim-resources.md); [1.1.5](researcher-role.md) (role gate) | **New 2026-06-22 (M)** — runtime-editable `tutorBlock` CMS over the YAML sim catalogue (Firestore overrides ⊕ YAML defaults); researcher-gated. Metadata only — no artefact-code path. Design |
| 1.1.43 | [activity-library-sharing.md](activity-library-sharing.md) | **P1 — PRE-PILOT (pulled forward 2026-06-24)** | ~6–9d phased (+~0.5d M3b researcher CRUD) | [1.1.19](teacher-activity-authoring.md) (M3 `duplicate`/`source_activity_id`); [1.A teacher-permission-model](../v1.0.0-pilot/implemented/teacher-permission-model.md) (Class + tagged ACL — the v2 deferral this fulfils); [1.1.41](teacher-sim-resources.md) (catalogue + adopt pattern); [1.1.5](researcher-role.md) (researcher role — extended here to write/delete on `activities`) | **New 2026-06-23 (M)** — activities as **class-independent, shareable resources**: own→many-classes **live-link**, **copy-on-adopt** across teachers, **draft/private/published** state. Fulfils the cross-teacher-sharing **v2 non-goal** both 1.1.19 + 1.A deferred. **M0 decouple** is independently valuable — also fixes *one concept activity per class* (activity id ≠ skill id). Generalises 1.1.19 M3 `duplicate`. **Sharing-scope gate RESOLVED 2026-06-24 (M): open trusted-set publish/adopt-copy, no pre-publish gate, researcher CRUD-over-all as moderation → pulled pre-pilot.** Design |
| 1.1.44 | [activity-image-materials.md](activity-image-materials.md) | **P1** | ~3–4d (M0–M3 core; M4 optional) | [1.1.7 student-multimodal-upload](student-multimodal-upload.md) (the proven `Part.from_bytes` "model sees an image" path); [1.1.25 curriculum-library](curriculum-library.md) (`MaterialRef`/`MaterialsSection`/`resolve_active_config`); [1.1.41 teacher-sim-resources](teacher-sim-resources.md) (resource-attached-to-activity pattern); ADR-001 + ADR-008; **JB image-retention posture** (teacher-supplied) | **New 2026-06-23 (M)** — a teacher attaches an **image** (diagram/graph/photographed worksheet) to an activity and the **tutor sees the pixels** during the student conversation — *not* OCR'd text. Closes the gap that the "add document" component (`MaterialsSection`) only feeds the **text/RAG** pipeline. **Reuses the ADK ArtifactService as the store**, keyed by an **activity slot** (`aipla/{teacher_uid}/{activity_id}/activity-image:*`); a `before_agent` **loader** copies the image into the student session at session start, a `before_model` **injector** inlines it as an image Part — both **twins of the shipped document pipeline**, the one new line being an image Part instead of a text one. `MaterialRef` gains a `kind:"curriculum"\|"image"` discriminator. Design |
| 1.1.45 | [rich-document-workbench.md](rich-document-workbench.md) | **P1** | ~6–9d phased (M0 rich-render ~0.75d; PDF viewer + editor are greenfield) | [1.1.33 documents-workbench-surface](documents-workbench-surface.md) (**completes its open M3**); [1.1.44](activity-image-materials.md) (images render + dual-audience GET-bytes pattern); [1.1.38](activity-elements-palette.md) (solution editor = a new element); [1.1.7](student-multimodal-upload.md) (upload path); **JB/AR on the two activity types + the editor feedback prompt**; **dep sign-off** (pdfjs / TipTap via the security gate) | **New 2026-06-24 (M + JB mockups [workbench-JB-1/2](../mockups/))** — make the student workbench's documents **rich**: parsed docs render via `ChatMarkdown` (markdown/tables/SVG/**KaTeX math**, not a `<pre>` dump — the cheap M0 win that completes 1.1.33 M3), a **Documents tab** (Radix Tabs, already installed) aggregating teacher materials + student uploads, a real multi-page **PDF viewer** (page-nav/zoom/fullscreen/download — JB-1 "din fil") with an **active file** the tutor critiques (→ session `document_ids`), and a **rich-text solution editor** (TipTap: B/I/U/lists/link/image/**fx math**/undo — JB-2 "din løsning"). **Core "rich output" = M0–M2; M3 (upload-your-file feedback) + M4 (editor) are the larger mockup builds (M4 may split to 1.1.46).** Thin-client + bundle costs declared (lazy-load the greenfield deps). Net axiom +8. Design |

### 23 June demo — latency + observability follow-up (added 2026-06-24, M)

Source: the 23 June demo felt janky; backend log triage found 19–36s `/stream` turns (dead-air thinking before first token) and that **app INFO logging — including the TTFT breakdown — was silently dropped** in the deployed container. Two direct fixes already shipped (anonymous-group **token silent refresh** so stale student tokens self-heal; root `setLevel(INFO)` so the TTFT line emits). The doc-worthy item is the **configurable thinking budget**.

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.46 | [thinking-budget-configuration.md](thinking-budget-configuration.md) | **P1** | env layer ✅ ~0.5d; per-skill ~0.5d; per-turn ~1d; persona ~1–2d | TTFT instrumentation ([observability/timing.py](../../../../backend/observability/timing.py)) now emitting (logging fix); [tutor-personas.md](tutor-personas.md) / [voice-personas.md](voice-personas.md) for the persona "thinking depth" layer | **New 2026-06-24 (M)** — make Gemini's thinking budget a deliberate, observable, per-context choice instead of a hard-coded `-1` (unbounded dynamic → 19–36s TTFT). **Layered**: env `AIPLA_THINKING_BUDGET` (✅ shipped, dev=0) → per-skill `SkillMetadata.thinkingBudget` → per-turn difficulty routing (`_should_think`/`_HeuristicRouter`) → persona "thinking depth" + custom personas. Pairs with the TTFT logging fix that makes the impact measurable. Net axiom +5. **Partially implemented** |

### 24 June — prompt transparency + configuration (added 2026-06-24, M)

Source: M's steer while resolving the [1.1.45](rich-document-workbench.md) M4 feedback-prompt gate — *"not just these prompts but throughout the teaching system we need transparency and/or configuration possible"*, with config at a **researcher** or **teacher** role and **custom personas** as the advanced teacher path. A **unifying** doc, not greenfield: the researcher role is **shipped** ([1.1.5](researcher-role.md)/ADR-016), transparency is **designed** ([1.1.27](lesson-author-surface.md) resolved-prompt preview), and config-over-prompt prior art exists ([1.1.20](tutor-personas.md) `interaction_style`, [1.1.12](voice-personas.md) personas, [1.1.42](sim-catalogue-admin.md) `tutorBlock` CMS, [1.1.32](teacher-ux-refinement.md) preamble visibility). It makes transparency + config **first-class and system-wide** so the next prompt is configurable by construction.

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.47 | [prompt-transparency-and-config.md](prompt-transparency-and-config.md) | **P1 — research enabler (post-pilot / v1.2)** | ~5–8d phased (M0–M1 transparency ~3.5d; M2 researcher edit ~2.5d; M3 teacher surfacing ~2d) | [1.1.5 researcher-role](researcher-role.md) (SHIPPED — built ON, not built); [1.1.27](lesson-author-surface.md) (`assemble_prompt()`+provenance generalised); [1.1.20](tutor-personas.md)/[1.1.12](voice-personas.md)/[1.1.42](sim-catalogue-admin.md)/[1.1.32](teacher-ux-refinement.md) (config prior art); **JB reveal-policy (M1, from 1.1.27); AR teacher-override surface (M3)** | **New 2026-06-24 (M).** **One model for every prompt the tutor runs on.** A **prompt registry** (inventory-as-data, like 1.1.38 `ELEMENT_REGISTRY`); **transparency** = generalise 1.1.27's resolved-prompt preview to *all* layers (`GET /resolved-prompt` → resolved text + provenance, role-gated); **config** = one Firestore-override⊕default store merged at session start, layered `researcher → persona(custom = teacher advanced) → teacher fragment(basic) → {teacher_focus}`; **role model builds on shipped 1.1.5** (researcher edits underlying layers; teacher via interaction_style+goal and **custom persona**). **M4's feedback prompt (1.1.45) is the first migrant** — ships hardcoded-as-a-registry-layer, becomes researcher-overridable here with zero rework ("migrate next sprint"). Seed pipeline formalised: seed writes defaults, never clobbers overrides. Net axiom +9. Design |

### 24 June — student-submission surfaces (added 2026-06-24, M)

Source: M's 2026-06-24 review of the shipped [1.1.45](rich-document-workbench.md) workbench — two design mistakes to fix **before the pilot**: (1) document-feedback shipped as a `workbenchType` **mode** that pre-empts the standard workspace (inconsistent with the solution editor, which is an **element**; caused the materials-vanish bug); (2) the solution editor is a **TipTap rich-text editor** expecting LaTeX / URLs / image-URLs — wrong for students, who hand-write physics. One fix for both: student-submission surfaces become **composable elements** whose output is an **image the multimodal tutor sees** (reusing [1.1.7](student-multimodal-upload.md)).

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.48 | [student-submission-surfaces.md](student-submission-surfaces.md) | **P1 — pre-pilot correction** | ~3–5d phased (reconcile ~1d; photo solution ~1d; whiteboard ~1.5–2d; TipTap removal ~0.5d) | **Amends [1.1.45](rich-document-workbench.md)** (supersedes M3b mode + M4 TipTap); [1.1.38](activity-elements-palette.md) (registry); [1.1.7](student-multimodal-upload.md) (**the image→tutor path reused**); [1.1.44](activity-image-materials.md); [1.1.32](teacher-ux-refinement.md); **JB/AR** image-vs-caption + feedback-prompt-on-image; whiteboard lib via `make security-check` | **New 2026-06-24 (M).** **Image-in, composable.** M0: document-upload → a `document` **element** (delete the `workbenchType="document"` pre-emption + builder mode-hiding; retire `"document"` from `WorkbenchType` — continues 1.1.32's "make workbenchType honest"). M1: the **solution** element becomes a **photo** of pen-and-paper work (reuses 1.1.7 resize + 1.1.21 guardrail); submit = a native multimodal turn, so the tutor *sees* it and the 1.1.45 feedback prompt critiques the image. M2: a **freehand whiteboard** (canvas → `toBlob` → same path) for no-paper cases. M3: **remove `@tiptap/*`** + the markdown serialiser (smaller bundle + dep/security surface). Net axiom +7. Design |

### 26 June — external-host MCP App rendering (added 2026-06-26, M)

Source: M's realisation that the sims we build as MCP Apps should render in *other*
clients (Claude Desktop, ChatGPT), then *"make it so for the cloud URL … document it
first and prove it."* A **breadth probe** (memory `aipla-breadth-over-depth`): proves
the sims are portable, vendor-neutral research instruments. A standalone proof-of-concept
shipped 2026-06-26 (renders all 3 sims in external hosts, verified end-to-end); this row
is the production cloud-URL path. **Not pilot-blocking; high demo/dissemination value.**

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.49 | [external-host-mcp-apps.md](external-host-mcp-apps.md) — sprint: [external-host-mcp-apps-sprint.md](external-host-mcp-apps-sprint.md) | **P2 — breadth probe** | ~1.5–2.5d phased (Phase 1 transport ~0.5–1d · Phase 2 `ui://` resources ~0.5d · Phase 3 optional ~0.5–1d) | [mcp_server.py](../../../../backend/protocols/mcp_server.py) (FastMCP `/mcp`); the standalone PoC `infrastructure/mcp-sandbox/external-host-demo/`; ADR-013; **JB/M sign-off on dev exposure of the 3 sims** (the `/mcp` endpoint is public/no-auth) | **SHIPPED dev 2026-06-26 (sprint EXT-MCP, M1+M2; Phase 3 deferred).** Cloud endpoint `…/api/mcp` live — anonymous Streamable HTTP, sims offered as `ui://` MCP Apps, `resources/read` returns the artefact HTML via lazy sandbox fetch; verified by `scripts/smoke-deployed-mcp.sh`. Visual render is host-dependent (Claude Desktop blocked by upstream claude-ai-mcp#165; ChatGPT/Inspector reliable). **New 2026-06-26 (M).** Serve the sims as `ui://` MCP App resources from the deployed cloud URL so Claude Desktop / ChatGPT render them as live interactive widgets (not text). **View half already done** (artefacts speak the SEP-1865 bridge); **PoC shipped + verified** (all 3 sims render in external hosts via stdio / Streamable HTTP). **Proven blocker:** the public `…/api/proxy/mcp` is currently **502 `backend_unreachable`** — the proxy forwards `/mcp` (no slash) but FastMCP 307-redirects to `/mcp/` and the consumed POST body can't replay; Next then 308-strips the slash. Phase 1 = a working public MCP route (dedicated route hitting `/mcp/`, or remove the redirect, or own origin) + a Streamable-HTTP smoke test; Phase 2 = fold the PoC's `discover_artefacts()` `ui://` registration into `mcp_server.py` (additive — skill tools unchanged); Phase 3 (optional) = `ui/notifications/tool-input` so the model can open a sim pre-configured. **Dev-only, open, for the workshop/research demo; public exposure on test/prod is an ADR follow-up.** |
| 1.1.50 | [authoring-teaching-framework.md](authoring-teaching-framework.md) | **P1 — research enabler** | ~2–3d on top of [1.1.39](activity-authoring-assistant.md) M0 (framework-as-a-layer ~0.5d · structure rubric + eval ~1d · researcher override store ~1–1.5d (mostly [1.1.47](prompt-transparency-and-config.md) M2 reuse) · version provenance ~0.5d) | [1.1.39 activity-authoring-assistant](activity-authoring-assistant.md) (OPEN — the chat panel + activity-mutating tools this steers); [1.1.47 prompt-transparency-and-config](prompt-transparency-and-config.md) (OPEN — the override⊕default store this rides, M2); [1.1.5 researcher-role](researcher-role.md) (**SHIPPED** — the gate); [1.1.38 palette](activity-elements-palette.md) (**SHIPPED** — what the rubric requires assembled); ALS-1/ALS-SHARE activity write API (**SHIPPED**); **AR/JB framework content (blocking); AR eval scoring key** | **New 2026-06-26 (M).** **Synthesis + feasibility-now, not a fourth assistant doc.** Answers "can the AI co-pilot be built now?" (**yes** — palette + activity write API + researcher role + tool primitives (`tool_context.state["user:id"]` + state writes) all **shipped** since 1.1.39 was written 2026-06-17) and "how do the *researchers'* pedagogical rules become a real editable artefact?". Defines the **teaching-framework meta-prompt**: a researcher-authored, **versioned, role-gated** layer = (a) the authoring agent's system prompt + (b) a **structure rubric** (objectives · prior-knowledge · Socratic scaffold · formative checkpoint · curriculum grounding) that *steers the [1.1.39](activity-authoring-assistant.md) tool-calls* AND is the **eval scoring key**. Rides [1.1.47](prompt-transparency-and-config.md)'s registry + override⊕default store (edit + version **without reseed**); generalises 1.1.39's static "Aswin meta-prompt" into a live layer; stamps `authoringFrameworkVersion` on authored activities for **cohort comparability**. M0–M1 ship the framework + eval statically with 1.1.39 M0; M2 (rides 1.1.47 M2) unblocks the live research loop. Net axiom +8. Design (OPEN). |
| 1.1.51 | [researcher-analytics-rollout.md](researcher-analytics-rollout.md) | **P1 — completes 1.1.5** | ~0.75–1d (P1 drill-downs ~0.25d · P2 overview `scope=all` ~0.3d · P3 transcripts ~0.15d; P4 analytics-chat ~0.5d gated/deferred) | [1.1.5 researcher-role](researcher-role.md) (**SHIPPED** — `is_researcher` + `assert_can_read_class`); [1.1.9 cost-dashboard](cost-dashboard.md) (**SHIPPED** — the cost half already admits researchers, pattern to copy); analytics aggregates + [analytics-chat-tools](../v1.0.0-pilot/implemented/analytics-chat-tools.md) | **New 2026-06-26 (M — "researchers need broad access; can they see all classes + teacher analytics + costs?").** Audit found classes/activities/cost ✅ but the **insights analytics** still owner-scoped — a researcher hits a **404 cliff** clicking into a non-owned class. 1.1.5 §Endpoints *specified* the analytics/transcript bypass; the sprint only shipped the class surfaces. Finishes it: per-class drill-downs (kpis/groups/activities/trend), `summary`/`compare` `scope=all`, per-group transcripts (consent-respecting). **The wrinkle 1.1.5 missed:** not a pure gate swap — the queries' defense-in-depth `allowed_group_codes` filter must also be widened (append-only, so owner output stays byte-identical) or the researcher gets authorized-but-empty. **P4 (analytics-chat cross-tenant) gated** — needs `is_researcher` plumbed into tool state + explicit sign-off on LLM cross-tenant reach. Writes stay owner-only. Design (OPEN → implementing). |
| 1.1.52 | [copilot-curriculum-selection.md](copilot-curriculum-selection.md) | **P2** | Phase 1 ~0.5d · Phase 2 ~2–3d (sequenced) | [1.1.25 curriculum-library](curriculum-library.md) (`CurriculumDoc`, RAG corpus, `MaterialsSection`, `aiplatform curriculum` CLI); the shipped `attach_material` co-pilot tool (1.1.39 thread, commit `0634629`); ADR-010 | **New 2026-06-30 (M — during the `attach_material` verify: "how does the co-pilot know what's in each curriculum library — a good description, or peek and read?").** The co-pilot picks curriculum docs by **metadata only** (title/level/topic/origin); `level`/`topic` are optional + often empty and there is **no summary field**, so it can be blind to a filename. **Phase 1 (build-ready, ~0.5d):** a `CurriculumDoc.summary` generated at ingest, surfaced in `attach_material`'s `available` list + the teacher's `MaterialsSection` browse + an `aiplatform curriculum summarize` backfill — cheap, helps the co-pilot AND the teacher's manual browse, touches no retrieval. **Phase 2 (sequenced later, ~2–3d):** corpus-wide semantic search + peek/read so the co-pilot matches on **content** — but it crosses the deliberate *"open corpus never reachable"* retrieval boundary ([curriculum_retrieval.py](../../../../backend/adk/curriculum_retrieval.py)), so it's **teacher-scoped** (own + shared cleared) and needs a clearance/privacy sign-off (JB/M). Order is metadata→summary→search because the human-in-the-loop (propose→Apply) means summary-grade signal suffices until the library is large. Design. |
| 1.1.53 | [group-shared-session-sync.md](group-shared-session-sync.md) | **P1 — correctness. M0–M3 SHIPPED 2026-07-01** | M0 turn-lock + M1 live chat sync + M2 (RESCOPED: "shares flow through the chat" — a labelled workbench push bumps the revision, NOT live workbench mirroring) + M3 presence ("N in your group are here"). Fixes bugs #1–#4. True co-editing of one shared artifact → [living-concept-map.md](living-concept-map.md). | The shared-uid group-session model ([`group_id_auth.py:_synthesize_uid`](../../../../backend/auth/group_id_auth.py#L304), [`group_sessions.py`](../../../../backend/db/group_sessions.py)); the group-scoped signal-doc + poll precedent ([1.1.29 call-teacher](call-teacher.md), [`group_signals.py`](../../../../backend/db/group_signals.py)); the poll-while-active live idiom ([1.1.31 M2 live-group-drilldown](live-group-drilldown.md)); ADR-001 | **New 2026-07-01 (M — from a code-level verification of "two kids, same group ID, both have the app open: does it sync?").** The primary classroom shape (a group on **separate devices**, one activity) has 4 verified bugs: **(1)** no live sync — messages don't reach a groupmate's screen until reload ([`useSessionMessages.ts:159`](../../../../frontend/src/hooks/useSessionMessages.ts#L159) fetches once, no poll/`onSnapshot`, AG-UI stream is per-turn); **(2)** ghost context — the shared backend session means the tutor answers against turns a groupmate never saw; **(3)** a concurrent-turn race — two parallel `ADKAgent.run()` on one session, no lock; **(4)** workbench clobber — last-write-wins on shared iframe-context state. **Decisions (M):** one **shared live thread**, **single group voice** (no per-student attribution → no new data model), **soft turn-lock** (one turn at a time; others queue). **Fix = the group is one logical user on N screens**, reusing the existing poll idiom (zero-LLM): **M0** transactional turn-lock on the existing `group_sessions` doc (409 on collision, 90s TTL steal) fixes #3; **M1** a tiny `GET …/pulse` + refetch-on-revision + composer lock/queue fixes #1 (and #2 falls out); **M2** workbench-revision sync fixes #4; **M3** optional presence count. No new collection, one new endpoint. Design. |

### 4 July — shared MCP App guest bridge (added 2026-07-04, M)

Source: 2026-07-04 workshop — the ChatGPT-rendered sims *display and interact* but
the model never sees a committed value ("push Afspil → AI blind"), while the AIPLA
app sees it fine. M: *"can we make a general solution … refactor them and be used
by all new mcp apps in the future … so we always have compatibility with both
ChatGPT and our local app?"* Diagnosis verified live in ChatGPT developer mode.
Directly follows / corrects [1.1.49](external-host-mcp-apps.md); complements
[unified-sim-rendering.md](unified-sim-rendering.md) (host-side render) by
unifying the **guest-side** emit bridge. **Not pilot-blocking; high demo value.**

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.54 | [shared-mcp-app-bridge.md](shared-mcp-app-bridge.md) | **P2 — breadth probe** | ~1–1.5d (bridge extraction ~0.25d · build+inline+CI drift-guard ~0.4d · re-inline 3 sims + `_template` ~0.15d · scaffold/skill update ~0.2d · verify ~0.25d) | [1.1.49 external-host-mcp-apps](external-host-mcp-apps.md) (SHIPPED — the `ui://` MCP App path this rides); [unified-sim-rendering.md](unified-sim-rendering.md) (complementary — host-side render); ADR-013 (static-artefact security model); the `mcp-app-artefact` skill (updated). No backend / server-metadata / frontend-app change. | **New 2026-07-04 (M).** **One canonical guest bridge, inlined into every artefact at build time, delivering each commit to whichever host is present.** Root cause is *two* problems: **(1)** ChatGPT's OpenAI Apps SDK ignores the SEP-1865 `ui/update-model-context` postMessage — it exposes `window.openai` (`setWidgetState` + `sendFollowUpMessage`, both verified against the Apps SDK reference); **(2)** the `emit()` + handshake bridge is hand-inlined and **drifted across 4 files** in **2 incompatible formats** — the 3 live sims speak JSON-RPC, but `_template` (what `new-artefact.sh` clones) still emits the pre-SEP-1865 flat `{source,type}` shape the current frontend **silently ignores** (a new sim is broken in the AIPLA app *and* ChatGPT). So the fix isn't "patch `window.openai` into 4 files" (that's the 5th variation) — it's **extract one `aipla-mcp-bridge.js`, inline it via `make sim-build` / `aiplatform sim build`, guard drift in CI (`--check`), and stop the skill + scaffold hand-copying it.** Corrects [1.1.49](external-host-mcp-apps.md)'s "content fix" (that `content` block rides the message ChatGPT never reads — render-only verification, never proved the model saw the interaction). Net axiom **+7**, 0 conflicts. **SHIPPED + VERIFIED dev 2026-07-04** (ChatGPT commit-visibility confirmed live by M; follow-ups: per-sim `label` broadcast fix + `check-artefact-broadcast.mjs` guard, and bridge `reportSize`/`initialState` from the `mcp-app-deploy-test` cross-check). |

### 8 July — external-host research capture (added 2026-07-08, M)

Source: the 1.1.54 ChatGPT/Copilot testing thread — once the sims broadcast to
external hosts, the recurring question was *"how do we get those interactions into
research?"* The `mcp-app-deploy-test` skill surfaced the mechanism (the `callTool`
mutation round-trip is the only widget→host channel that reaches our server).
**Design exploration, gated on an identity/consent (ADR) decision — not build-now.**

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.55 | [external-host-research-capture.md](external-host-research-capture.md) | **P2 — DEFERRED / backlog** | ~1.5–2d mechanism; identity/consent = ADR | [1.1.54 shared-mcp-app-bridge](shared-mcp-app-bridge.md) (the dual bridge this extends — adds the 3rd, server-observable channel); [1.1.49 external-host-mcp-apps](external-host-mcp-apps.md) (`/api/mcp`); ADR-001 (anon-group identity = the crux) | **DEFERRED 2026-07-08 (M — not currently scoped; captured for a future discussion). Likely direction (§0): external hosts = *advertising/discovery*; a deep-link CTA (`openExternal`/`setOpenInAppUrl`) sends users into the deployed app for full teaching+support — which sidesteps the identity/consent problem (auth/consent happen in-app post-click). Per-student in-host research capture stays the gated tail.** New 2026-07-08 (M).** External-host (ChatGPT/Copilot) sim interactions never reach AIPLA — the `setWidgetState`/`sendFollowUpMessage` channels are host-only; only a widget-initiated **`callTool`** routes back through our `/api/mcp` server. Proposes a `record_interaction` action tool (`openai/widgetAccessible`) + an `AIPLA_BRIDGE.callServerTool` channel (no-op in the AIPLA app → no double-log). Hard half is identity (anon-group has no external-host session) + consent (GDPR) — deny-by-default, anonymous-aggregate first, opt-in join-code binding later. Net axiom **+5** (SECURE BY CONSTRUCTION −1, justified). Design (OPEN, Phase 0 gated). |
| 1.1.56 | [researcher-copilot-log-access.md](researcher-copilot-log-access.md) | **P1 — GOVERNANCE-GATED** | ~2–3d eng, preceded by an unbounded consent/governance step | [1.1.5 researcher-role](researcher-role.md) (**SHIPPED** — `is_researcher`/`assert_can_read_class`); [1.1.51 researcher-analytics-rollout](researcher-analytics-rollout.md) (sibling — but that's STUDENT data; this is a different data class); [teacher-coworking-copilot.md](teacher-coworking-copilot.md) Part 4b (session tagging + teacher history — built jointly); the chat-log→BigQuery pipeline; **M + AR/JB + UCPH data-protection sign-off (the gate)** | **New 2026-06-30 (M — during the co-pilot verify: "this data should be available for researcher").** Teacher co-pilot conversations (authoring/manage/analytics) are **captured** (ADK sessions + Firestore `chat_sessions` mirror) but reach **neither** the research store (the chat-log→BigQuery pipeline drops non-student sessions — [callbacks/session.py](../../../../backend/adk/callbacks/session.py) `if not group_code: return`) **nor** any reader (only single-thread resume ships; no history list; researcher access is student-only). **The long pole is a governance gate:** exposing teacher AI-assistant conversations to researchers is a GDPR + UCPH ethics decision (legal basis, teacher notice/consent), owned by M + AR/JB + UCPH data-protection — the repo doc treats it as a **hard precondition** before the pipeline flips. Engineering (~2–3d): emit teacher co-pilot turns on a **DISTINCT stream** (never mixed into student-research tables); tag `ChatSessionIndex` with `kind` + teacher/class scope (shared with Part 4b); reuse the researcher auth pattern but **teacher-scoped, not class-scoped** (a real wrinkle); add a researcher transcript view + the teacher's own history list. **Split: the teacher-reads-own half is safe now; the researcher half waits on the gate.** Design (OPEN — governance-gated). |

### 10 July — competency rubrics (AR's 29-June sources, audited 2026-07-10)

Source: AR's 29-June email (competencies frameworks for teacher evaluation of GAI-assisted
student work + a Drive corpus of students'-difficulties literature), audited in the scoping
site — [`notes/2026-07-10-aswin-competency-sources-audit.md`](file:///Users/mark/Documents/clients/cph-uni/notes/2026-07-10-aswin-competency-sources-audit.md).
**This is AR's input to R1**: published, validated rubrics for scoring student *work* (MAPS
problem-solving, SAAR inquiry) — a lens family orthogonal to the ICAP+FCI vs CPS+DRA choice,
composing with either. Plus the Kohl four-format quiz template (a TAA M2 input) and a
literature-grounded misconception index for 2.5 Lens B.

| Order | Doc | Priority | Estimate | Dependencies / Gate | Notes |
|---|---|---|---|---|---|
| 1.1.57 | [competency-rubrics.md](competency-rubrics.md) | **P1 — offline slices un-gated; teacher-facing M5 R1-gated** | M0 judge proto ~2d + 0.5d CLI · M1 anchors ~2–3 ped-d (AR/JB) · M2 SAAR activity ~2d · M3 +1d on TAA M2 · M4 index ~2d · M5 ~1d (post-R1) | [2.5 session-analytics-rubric](../post-pilot/session-analytics-rubric.md) (extends its lens stack: + Lens C MAPS, Lens D SAAR); 1.2 BQ stream (shipped); SUBMIT-1 artifacts (shipped); [TAA](teacher-activity-authoring.md) M2 (quiz template lands there); **R1 gates only M5** (teacher-facing vocabulary) — same split discipline as 1.1.31 | **New 2026-07-11 (from AR's 29-June email).** Adds the **competency layer**: MAPS 5-category judge (CC-BY, embeddable) scoring **student-initiated artifacts only** — the *evidence-integrity rule*: tutor scaffolding generates NA scores by design, so the judge partitions transcript turns and prefers SUBMIT-1 photo/whiteboard uploads; per-activity **anchor packs** (~5 scored examples, AR/JB) or the lens abstains. SAAR testing-experiment rubric scores the **students-design-their-own-agent** activity 1:1 (refute-not-confirm is the key criterion; the paper's Tables X/XI are paste-ready few-shot). Kohl four-format isomorphic quizzes: assign formats (students choose badly), **mastery = ≥2 formats**, mapped distractors feed the misconception signal. Misconception index from AR's ~320-paper corpus widens Lens B beyond FCI's mechanics ceiling (optics/thermal/EM; quantum still open). PLIC/CLASS stay **outside the app** (PhysPort item security; official pipelines). Net axiom **+10**. Design (OPEN). |
| 1.1.58 | [curriculum-faceted-browse.md](curriculum-faceted-browse.md) | **P2 — teacher library ergonomics. M0–M5 ALL SHIPPED (2026-07-13/14)** | M0 search fix (SHIPPED `42417d9`) · M1 tags (SHIPPED — TAGS-1: field+facets+PATCH+CLI+FE chips) · M2 subject facet ~0.75d · M3 folders ~1.5d · M4 faceted-browse UX ~0.75d | [1.1.25 curriculum-library](curriculum-library.md) (`CurriculumDoc`, `curriculum_docs`, `MaterialsSection`, `aiplatform curriculum` CLI); [1.1.52 copilot-curriculum-selection](copilot-curriculum-selection.md) (the `summary` field the search leans on); the flat folder pattern from [`db/folders.py`](../../../../backend/db/folders.py) (reused as a UI-pattern reference, ACL re-keyed to `ownerScope`); ADR-010 (content-search boundary this stays clear of) | **New 2026-07-13 (M — teacher searched "atomer" in Materials, got zero results).** Root cause: the browse "search" was an **exact case-insensitive equality on the `topic` field only** ([curriculum.py:88](../../../../backend/db/curriculum.py#L88)) — never matched title/summary, and every topic-less teacher upload was unsearchable. **M0 (SHIPPED):** broadened to a multi-term substring match over title+topic+summary (no schema change, no indexing — the list already filters in Python post-ACL-fetch). **M1–M4 (Design):** the second ask — folders/tags/subjects — adds three optional owner-scoped fields to `CurriculumDoc` (`tags[]`, `subject`, flat `folderId`) + a `curriculum_folders` collection **keyed by `ownerScope`** so folder ACL can never diverge from doc ACL, and rebuilds the Materials filter bar as **search box + removable facet chips** (level/subject/tag/folder, AND across facets). Content/semantic search stays OUT (that's the RAG path + [1.1.52 Phase 2](copilot-curriculum-selection.md) clearance boundary); flat folders only (no nesting); subject is a soft vocab (chips + free entry). All new fields optional/default-empty → no backfill. Net axiom **+7**. Design (OPEN, M0 shipped). |

| 1.1.58 | [teacher-account-defaults.md](teacher-account-defaults.md) | **P2 — un-gated** | ~1–1.5d (M0 prefs store+API ~0.4d · M1 Defaults card ~0.5d · M2 seeding + tri-state flag hook ~0.5d) | [1.1.32 teacher-ux-refinement](teacher-ux-refinement.md) (SHIPPED — the no-control-without-a-consumer rule); [1.1.12 voice-personas](voice-personas.md) (SHIPPED — the per-class persona being seeded); RUBRIC-1 M3 (SHIPPED — the settings-pane cohabitant) | **New 2026-07-11 (M — "teachers don't have any settings currently... defaults on language etc?").** Teachers get their first account-level settings: `teacher_prefs/{uid}` with default activity language (seeds the builder on /new), default class persona (applied at class create; ClassPersonaPanel stays the per-class override), and a **beta-features opt-in** — the tri-state flag graduation path (`'1'` on-for-all · `'beta'` runtime teacher opt-in · `''` off) from build-time NEXT_PUBLIC dark flags. Defaults SEED contextual controls at create-time only, never fight them (hydrate/edit wins). Only consumers-that-exist get controls (the dead-`difficulty` lesson). Net axiom **+7**. Design (SETTINGS-1 executing 2026-07-11). |

| 1.1.59 | [curriculum-scale-performance.md](curriculum-scale-performance.md) | **P1 — scale correctness. SHIPPED 2026-07-13 (M1–M3)** | M1 shared-corpus cache ~0.75d · M2 pagination ~0.5d · M3 FE debounce+load-more ~0.5d | [1.1.25 curriculum-library](curriculum-library.md); [1.1.58 curriculum-faceted-browse](curriculum-faceted-browse.md) (hardens the same browse path; absorbs its deferred M4 debounce); [`db/firestore.py`](../../../../backend/db/firestore.py) query helper | **New 2026-07-13 (M — "is the search efficient? we're about to add thousands of docs").** Root shape: [`list_curriculum_for_teacher`](../../../../backend/db/curriculum.py#L65) **fetches the entire ACL set, deserializes every row, filters in Python, unbounded, per request** — cost is O(corpus), not O(result), so a browse over a 3k-doc shared corpus is ~3k Firestore reads × keystrokes × teachers. Fix (no external index — free-text-over-content stays the RAG path, and Firestore has no substring op): **(M1)** a read-through, TTL-bounded, invalidate-on-write **in-process cache for the SHARED corpus only** (read-mostly, identical across teachers; keyed to `ownerScope==shared` **by construction** so it can never hold/leak a teacher's private docs — own docs stay live for freshness); **(M2)** **pagination at the endpoint** (limit≤200/offset + `total`) leaving the internal full-list function intact for the co-pilot/summarize/query callers; **(M3)** FE **250ms debounce + "Showing X of Y" + Load more**. Steady-state shared browse → **0 reads/TTL**. Net axiom **+7**. Design (OPEN). |

## Build status — verified 2026-06-08

Verified by code inspection on 2026-06-08 (not by doc placement, which lags). Legend: **SHIPPED** (in `dev`, evidence cited) · **PARTIAL** · **OPEN** (designed, not built).

| Row | Item | Status | Evidence / still-to-do |
|---|---|---|---|
| 1.1.1 | tutor-verbosity-fix | **SHIPPED** | SKILL.md constraint blocks across 3 tutors + pytest guard (QUICK-WINS-V11, `9d03c61`) |
| 1.1.2 | proactive-sim-reactive-tutor | **SHIPPED** | `/proactive-event-check` gate + FE sentinel, M1–M10 (2026-06-03) |
| 1.1.3 | student-consent-prompt | **OPEN** | no `consent_given` in code. Gated on JB wording |
| 1.1.4 | session-report summary-primary | **SHIPPED** | 2026-06-13 — `reports/narrative.py` grounded Gemini-Flash narrative (workbench-grounded, "the group", no-emoji), generated on-demand + cached on `ChatSessionIndex` (`summary_text`/`…_generated_at`/`…_based_on_turn_count`), regenerated when turn count grows, best-effort (failure → report still renders). Wired into `/api/reports/*` (`narrative=true` default; `source=bq` verify stays LLM-free). FE flip: prominent **Summary** (ChatMarkdown) + **At a glance** metrics; transcript collapses behind **View full transcript** (localStorage-persisted); lesson transcript (`GroupTranscriptSection`) already mounted below. Sprint [implemented/session-report-summary-primary-sprint.md](implemented/session-report-summary-primary-sprint.md). **Deferred** (need 1.1.3 consent): server `transcript.csv` + 410 + consent badge — client CSV/JSON already works. Backend 1940 + FE 1044 tests green |
| 1.1.5 | researcher-role | **SHIPPED** | 2026-06-13 — `User.is_researcher` from the `role:researcher` Firebase claim; `assert_can_read_class` (researcher bypass + OTel `auth.researcher_bypass`); `GET /api/classes?scope=all` (researcher-only, 403 otherwise) + read-route bypass (`_load_readable`, write/delete stay owner-only); admin `grant-researcher`/`revoke-researcher` endpoints (claim-merge) + `aiplatform users` CLI; FE `useIsResearcher` + Research-view toggle on the class list. Sprint [implemented/researcher-role-sprint.md](implemented/researcher-role-sprint.md). `logs --all-classes` descoped (ADR-005: researchers query BQ directly via `logs schema`). Backend 1909 + FE 1039 + CLI 101 tests green |
| 1.1.6 | group-code-ttl rename + archival | **SHIPPED** | `DEFAULT_GROUP_CODE_TTL_DAYS` + 410-on-expiry (QUICK-WINS-V11) |
| 1.1.7 | student-multimodal-upload | **SHIPPED** | 2026-06-11. Native AG-UI multimodal (images ride `UserMessage.content` as `ImageInputContent` → ADK Parts, retained in session history; docs → docparse). Composer paperclip+camera, on-device person guardrail (1.1.21), client resize, units-loop preamble. M1→M3 incl. the native-rewire that deleted the first custom-injector pass. Sprint `MMU-1`. Commits incl. `6fbbe92`→`1fdb3a0` |
| 1.1.8 | exit-ticket | **OPEN** | gated on JB/AR question set |
| 1.1.9 | cost-dashboard | **SHIPPED** | 2026-06-13 — code rate-card (`analytics/rate_card.py`) + spend queries (`analytics/cost_queries.py`, keyed on `group_id`, EUR-folded); `GET /api/classes/{id}/spend` (owner or researcher via `assert_can_read_class`) + `GET /api/insights/cost` (researcher-only cohort/model/per-class); `Class.cohort` field + PATCH; FE `BudgetPanel` on class-detail (this-month/projected/top activities+groups) + researcher **Cost** tab in `InsightsTabs` (`/teacher/insights/cost`). Multimodal cost folds into `token_in` (no new column). **Cap progress bar deferred** (no class-level cap; enforcer is skill-level). Sprint [implemented/cost-dashboard-sprint.md](implemented/cost-dashboard-sprint.md). Backend 1932 + FE 1044 tests green |
| 1.1.10 | teacher-choice-ttl | **OPEN** | backend `mint_group(ttl_days)` pre-existed; route-validation + teacher-UI form field + CLI `--ttl-days` not built |
| 1.1.11 | voice-provider-abstraction | **SHIPPED** | `backend/voice/` (base, providers/, registry, cache, cost) |
| 1.1.12 | voice-personas | **SHIPPED (core bundle)** | `VoiceStatusPill` + the full persona bundle (2026-06-10/11): `Persona` model + 6 Danish-educator YAML defs (`backend/personas/`, real avatar images in `frontend/public/personas/`) + loader + `GET /api/personas`; builder **persona-card picker** that ties `interaction_style` + records the persona; **student chat bubble shows the persona's avatar + name**; **tutor read-aloud uses the persona's voice** (precedence: persona > class > skill > env). Commits `567d986`→`3b141b8`. **Pick Astrid → rigorous + her voice + her face.** **Remaining:** optimise the ~2MB avatar PNGs → webp, custom (Firestore) personas, `aiplatform personas` CLI, VoiceStatusPill friendly-mode |
| 1.1.13 | sim-onboarding-ergonomics | **SHIPPED** | `useArtefactReportEvent` denylist (2026-06-04) |
| 1.1.14 | chat-history-flicker-on-token-refresh | **SHIPPED** | AGUIProvider gate fix (2026-06-04) |
| 1.1.15 | chat-svg-streaming-placeholder | **SHIPPED** | `SVG_STREAMING_TAIL_RE` (2026-06-04) |
| 1.1.16 | security-monitoring-pipeline | **SHIPPED** | ci.yml `security-audit` + weekly cron + skill (2026-06-05) |
| 1.1.17 | student-engagement-signals | **OPEN** | design-only (2026-06-06); no BQ `paste_ratio`/`revision_count` columns. Gated on JB/AR metric review |
| 1.1.18 | voice-pronunciation-config | **SHIPPED** | `frontend/src/lib/voice-pronunciation/` — see Numbering note below |
| 1.1.19 | teacher-activity-authoring | **PARTIAL (M0+M1)** | M0+M1 shipped (TAA-1 sprint, `3f65f97`→`baeaae5`; no-workbench concept activity end-to-end). Remaining M2 quiz / M4 workbench-type gated on JB/AR; M6 equipment co-design. Status was "OPEN" — corrected 2026-06-19. Umbrella for non-sim teacher activity creation, phased |
| 1.1.20 | tutor-personas | **SHIPPED (core)** | 2026-06-10 — `interaction_style` field (socratic default / concise / rigorous / warm) end-to-end: override-preamble injection in the agent chain (socratic = passthrough, zero regression) + builder picker. Commits `d1015c1` + `5eb6d0a`; 8 BE + 1 FE tests. **Follow-ups:** AR's final concise/rigorous/warm wording, OTel `tutor.interaction_style`, socratic SKILL.md extraction. A persona (1.1.12) will resolve down to this field |
| 1.1.21 | multimodal guardrail + units loop | **SHIPPED** | 2026-06-11 with 1.1.7. On-device `FaceDetector` no-person guardrail (graceful-degrade, never over-blocks) + units-loop guidance as a shared `image_input` preamble. M's posture = on-device private default |
| 1.1.22 | end-of-class-notes-summary | **OPEN** | design-only (2026-06-09). Composes 1.1.7 + 1.1.8; sequenced after both |
| 1.1.23 | bidirectional-voice-brief | **SHIPPED** (ahead of the 06-23 date) | 2026-06-11. Sprint `VOICE-IN-REC`: M1 `GCPSTTProvider` + `/api/voice/stt/transcribe`; M3 composer mic (talk-to-type **XOR** record-lesson); M4 config-simplification (raw picker → "Advanced", two capability toggles). `gemini_live` stays deferred. Voice-in **on by default** per class; recording is a deliberate consent-gated toggle. Commits `cbdb8c5`→`4b21dfb` + `10221a4` |
| 1.1.24 | offline-lab-workbench | **OPEN** | design-only (2026-06-09). Gated on JB/AR ground-truth model + 2 real experiments; depends on 1.J Type 5 |
| 1.1.25 | curriculum-library | **SHIPPED (M1–M5)** | 2026-06-11 M1 (`CurriculumDoc` + CRUD + `GET /api/curriculum` ACL browse). 2026-06-12 M2 (`bootstrap_rag_corpus.py` + `db/rag_corpus.py` + `POST /ingest` AILANG Parse→RAG), M3 (`MaterialRef` + `build_curriculum_retrieval_tool` `VertexAiRagRetrieval` scoped to cited docs + grounding preamble, wired into `create_agent`), M4 (builder Materials picker `MaterialsSection` + `curriculumApi.ts` + `materials` passthrough), M5 (`POST /api/curriculum/query` + `aiplatform curriculum ingest/list/query` CLI). Sprint `CURRICULUM` (commits `3c3153a`→`b21d95e`, M2–M5 follow). Backend 1888 + FE 1031 + CLI 99 tests green. **Real-world activation pending:** run `bootstrap_rag_corpus.py` against `aipla-dev-2026` + set `CURRICULUM_RAG_CORPUS_NAME` (docs/ops/gcp-side-effects.md) — everything degrades gracefully until then. Shared-corpus copyright clearance (JB/M) + B/C parsing remain product gates for *seeding*, not code |
| 1.1.26 | teacher-ui-consolidation | **SHIPPED (P1–P5)** | 2026-06-09 — full foundation: P1 primitives (`components/teacher/ui/`), P2 by-breakpoint nav (`TeacherNav`), P3 class-detail onto primitives + "Class settings", P4 real Activities library, P5 Insights unification (`InsightsTabs`). Commits `6bb33af`→`d4692ee`; 958 FE tests. **Builder Essential/Advanced section-model intentionally deferred** to when its config features (1.1.20/1.1.23) are built into it |
| 1.1.27 | lesson-author-surface | **OPEN** | design-only, **re-scoped 2026-06-12** to two net-new pieces: resolved-prompt preview (source-attributed, read-only) + trial session (`is_trial`, analytics-excluded). Dropped knobs/curriculum/chat-skill → 1.1.20/1.1.25/1.1.19. ~2d. Gated on JB sign-off on what the preview reveals |
| 1.1.28 | teacher-ui-de-mock | **SHIPPED** | 2026-06-15 — made the de-mock an *explicit* item (it had been riding implicitly inside 1.1.19/1.1.26, which is how mocked corners shipped). (a) Activity editor `[id]` wired to **real** `/api/activity-configs` (loads by `classId`+`activityId`; honest loading/empty/error states) + reachable nav (Activities-list "Configure", new→edit handoff) — this lit up the dark 1.1.25 curriculum Materials picker. Commit `25dcf26`. (b) **Deleted `frontend/src/app/teacher/_mock-data.ts` entirely**; group report `[groupId]` no longer falls back to a fabricated session (404→empty, error→error; dropped the "mock data" badge). Tests rewritten to real-shaped payloads. Commit `30d6aae`. (c) Standing guard `check:no-mock` in `quality:check{,:fast}` so mock can't creep back into shipped surfaces. 68 teacher FE tests + full `quality:check` green. **Lesson:** "no mock fallback in shipped surfaces" is now a definition-of-done gate, not an assumption |
| 1.1.29 | call-teacher | **OPEN** | design-only (2026-06-15). Build-ready, no human gate for the minimal raised-hand slice (M0+M1). New `group_signals` doc + student `CallTeacherButton` + teacher Calls strip on the existing class poll |
| 1.1.30 | mobile-performance-pass | **OPEN** | design-only (2026-06-15). Investigation-first; Phase 0 profiling note gates Phase 1 fixes. No perf tooling exists yet (no bundle-analyzer/Lighthouse/web-vitals) |
| 1.1.31 | teacher-analytics-framework (live dashboard) | **OPEN — R1-gated** | design-only (2026-06-15). Shell + raised-hand + deterministic signals un-gated (co-builds with 1.1.29); the rolling 5-min **summary** layer is gated on the R1 framework decision (before 2026-06-29 freeze). **Do not instrument the summary before R1** |
| 1.1.32 | [teacher-ux-refinement](teacher-ux-refinement.md) | **PHASE A SHIPPED; Phase B OPEN** | **Coherence/refinement doc** from M's live critique: collapse the `teacher×class×persona×activity×sim` sprawl to legible nouns. **Phase A shipped 2026-06-15** (live-review-driven): dead `difficulty` knob removed; "where settings live" `SettingsMap` (married to the sidebar nav); sim↔activity honesty (removed the lying "Paired workbench" knob — a sim IS the skill it runs); persona class-default-only (Q4) with full transparency (voice + `voicePrompt` + bio per card); **teaching-style enforcement made visible** (the literal injected preamble per style); teacher-only skills filtered from the student-lesson catalogue; **class view shows the activity title + Configure link** (assigned activities editable in place, matching the Activities page). **Deferred:** "Open as student" preview (own change — chat auth gating). **Phase B / the skill-config reuse follow-up (post-pilot, OPEN):** activity templates + class-assignment split so an activity is authored once and assigned to many classes, and "Add from catalogue" lists the teacher's own activities — see teacher-ux-refinement.md Phase B #6 + [post-pilot/teacher-artefact-parameters.md](../post-pilot/teacher-artefact-parameters.md) (2.3) for bounded per-artefact params. Net axiom +6. Amends 1.1.19/1.1.26/1.1.12/1.1.20 |
| 1.1.33 | [documents-workbench-surface](documents-workbench-surface.md) | **M0–M2 + M4 SHIPPED; M3 → folded into [1.1.45](rich-document-workbench.md)** | M0 `445422d`, M1 gallery `ca89a14` (+`e416369`), M2a/M2b per-material student visibility `7c923af`, M4 teacher parse-review `e2e0007` (stale "M0 only" corrected 2026-06-19). **M3 (docparse'd-document rich rendering) now lives in [1.1.45 rich-document-workbench](rich-document-workbench.md) M0** — superseded + expanded (rich render + Documents tab + PDF viewer + solution editor). Images also now render via [1.1.44](activity-image-materials.md). **New 2026-06-15** — one student-workbench **Documents** tab aggregating (a) student uploads (1.1.7), (b) teacher-assigned materials (1.1.25 `MaterialRef`, today RAG-only / invisible to students), (c) docparse'd docs. **Aggregation surface, not a subsystem** — reads AG-UI `ImageInputContent` / ADK session events + existing corpus metadata natively (no new store; framework-native check in doc). M0 image click-to-expand SHIPPED `445422d` (shared `ZoomableImage` lightbox). M1 uploads gallery (FE-only ~1d); **M2 assigned-materials-visible is teacher-controlled per material** (`MaterialRef.studentVisible`, default off — M's 15 June call; un-gates the old JB/AR question, RAG grounding still uses all cited docs); M3 docparse render. Net axiom +9. P2 |
| 1.1.34 | [human-tool-use-card-persistence](human-tool-use-card-persistence.md) | **SHIPPED** | 2026-06-16/17 — M1 (`03b0b66`, surface MCP-app `interactions[]` on `/messages`) + M2 (`cf5c620`, seed provider with read-only confirmed cards on restore). Stale status corrected 2026-06-19. **New 2026-06-16** — restore MCP-app interaction cards (Boldkast/LED-Planck/KineBot "Sendte spørgsmål med…") on reload. M's 16 June observation: live session shows the interaction + the tutor's reaction; reload shows only the reaction. **Read/render gap, not data-loss** — the interaction already persists as an ADK `state_delta` event (`append_event`, survives rejoin) but `_events_to_messages` is text-only so it's filtered out, and the `useHumanToolEvents` card store is page-lifetime. **No new store** (framework-native check in doc): enrich the iframe-context event with the client-computed `label`, surface a parallel `interactions[]` on the existing `/messages` read, seed the provider with read-only confirmed cards on restore (existing interleave-by-index render). Companion to 1.1.32 (student-side reload fidelity) + same no-new-store pattern as 1.1.33. ~1.5–2d. Net axiom +7. P2 |
| 1.1.35 | [research-audio-capture-quality](research-audio-capture-quality.md) — sprint: [research-audio-capture-quality-sprint.md](research-audio-capture-quality-sprint.md) | **CORE SHIPPED (M1–M3); M4–M6 OPEN** | **New 2026-06-16** — 16 June demo fix. Research-audio transcripts were near-unusable (~40% empty, rest garbled); a spike on the actual demo audio proved the recordings are FINE — the failure was the STT config (single-language `da` on code-switched, mostly-English speech) + 50s slicing optimised for a live-ness we don't need. **M1–M3 SHIPPED `23ca548`**: `GeminiSTTProvider` — transcribe via Gemini with a da/en verbatim grounding prompt; registers `gemini_<model>` so `VOICE_STT_PROVIDER=gemini_2.5-flash` routes the recording transcribe path through Gemini with no route change (spike: 12k accurate chars vs 360 today, ~8–16× cheaper than Cloud STT); Cloud STT removed entirely (it garbled the audio; no fallback — audio retained + re-transcribe on outage); `voice.stt` engine/bytes/status span + `transcriptEngine` provenance. **OPEN:** M4 retention TTL (research-audio bucket is script-created, NOT Terraform-managed — needs a bucket-management decision), M5 frontend recorder survive-reload (coverage), M6 whole-file re-transcription route + CLI (recovers `tilted-petal-71` + the eval harness). Net axiom +8. P1 |
| 1.1.36 | [session-report-dual-source-narrative](session-report-dual-source-narrative.md) | **SHIPPED (A1–A5)** | 2026-06-16/17 (stale OPEN corrected 2026-06-19) — A1 dual-source narrative (chat+transcript), A2 regen+5-min debounce, A3 inputs/"what's included" block, A4 background settle pre-gen, A5 named generating state + friendly group-time/source-material fixes. Commits `2c21e1b`→`d646dbf` (+ `74838fd`/`934e4a0`/`bac0d7e`). **New 2026-06-16** — extend the teacher-report narrative (1.1.4) to summarise **both** the chat history AND the pre-parsed audio transcript (1.1.35 — text, NOT raw audio: we already transcribe it; raw audio would re-transcribe per open + ~80k tokens/40-min session). On-demand generation made **transparent**: chat + transcript render immediately (no LLM wait) and stay **browseable** beneath the summary so the teacher reads the source while it generates; a "what's included" line (turns · audio-min · sims · model) explains the wait. Regen **debounced to ≤ once/5 min** on a live session (cost-guard on the teacher's own on-demand refreshes — NOT a proactive push; the rolling live summary stays R1-gated in 1.1.31) and now also fires when the **audio transcript** grows. Clarifies: *live* regen is lazy on the report GET (debounced); a **background "settle" job** (~20 min idle, `last_message_at` trigger) pre-generates the final summary so a **researcher** reviewing a finished class gets it instantly — AIPLA's first app-level scheduler (internal endpoint + Cloud Scheduler, post-hoc so un-gated). Net axiom +8. P1 |
| 1.1.37 | [student-audio-turns](student-audio-turns.md) | **PARTIAL — send-path shipped** | 2026-06-17 (stale OPEN corrected 2026-06-19) — `b365a14` shipped the send-path enabler: audio attachments become native `AudioInputContent` (tutor hears the audio). Remaining: parallel-transcribe for the chat bubble / `aipla_chat_turn` log + the student-UI record-and-send turn. **New 2026-06-16** — replace the broken dictation (STT→text→box, 1.1.11) with a **native audio turn**: the student sends the **audio itself** as the message; the Gemini tutor **hears it directly** (AG-UI `AudioInputContent` → `ag_ui_adk` → ADK Part — the 1.1.7 image-upload path, no new transport); transcribe **in parallel** (GeminiSTTProvider, 1.1.35) only for the chat bubble + the `aipla_chat_turn` log/review, so a transcription error no longer corrupts the tutor's input (it heard the audio). **Consent CLEARED** (signed waivers, demos/pilot) + per-class `voiceInput` gate. The `audio_message` mode of the 1.1.23 voice axis. Net axiom +9. P1 |
| 1.1.38 | activity-elements-palette | **M0–M4 SHIPPED (palette complete); M5 OPEN** | **New 2026-06-17.** Generalised the activity element layer into a bounded **element registry** + **4 new elements, all SHIPPED 2026-06-22 end-to-end** (backend model + registry + route/DB + student render + builder editor + tests each). M0: `ELEMENT_REGISTRY` + cap validator + frontend mirror + the add-element recipe. M1: **data table** (`WorkbenchTable` commits entered data to the tutor via `iframe-context` + `TableEditor`) + the uniform `elementRenderers` dispatch (n=2). M2: **chart** (`WorkbenchChart` SVG auto-plotting the table's first two numeric columns, reactive via `aipla:table-change` + `ChartEditor`). M3: **formula calculator** (`WorkbenchCalculator` + `CalculatorEditor` + hand-rolled `safeFormula.ts` evaluator — no `eval`, 13 adversarial cases, client-side; formula *content set* still a JB/AR input). M4: **note** (Markdown instructions/reference card reusing `ChatMarkdown` + `NoteEditor`) — **reinterpreted from "document"** (redundant with shipped `materials`+1.1.33). Q1 resolved (stack). Deferred: `ElementEditor` shell (n=1), per-column chart selection, scientific-calc mode, edit-page element editing. **OPEN:** M5 observability (element-interaction events → BQ, co-design with 1.1.17). **Follow-ups (2026-06-22):** quick-start **templates** (`activityTemplates.ts` + `TemplatePicker` — concept/lab+chart/calculator/energy-note) **SHIPPED** (never a blank builder); live in-builder **preview mode designed → 1.1.40**. |
| 1.1.39 | activity-authoring-assistant | **OPEN (design)** | **New 2026-06-17 (Aswin)** — teacher-facing ADK **authoring co-pilot** (chat panel in the builder); tools (`set_lesson_prompt`/`propose_checklist`/`add_element`/`suggest_materials`/…) write **accept/edit proposals** into the `ActivityConfig` draft (never auto-publish). Unifies 1.1.19 M6/M7/M8 as conversational tools. **⚠ teacher-auth stream** (the recurring 401 corner — `useTeacherAuth`). M0 (prompt co-pilot) gated on **Aswin's meta-prompt**; degrades to the manual builder when absent |
| 1.1.40 | [activity-preview-mode](activity-preview-mode.md) | **M0 SHIPPED; M1–M2 OPEN** | **New 2026-06-22 (M).** Live in-builder **workspace preview** — renders the authored elements (table/chart/calculator/note/checklist) **exactly as a student sees them**, instant + interactive, **no save / no session**, reusing the shipped `WorkspaceElements`. The **workspace half** of "see before publish"; **complements [1.1.27](lesson-author-surface.md)** (prompt provenance + trial *chat*). **M0 SHIPPED 2026-06-22**: `builderToElementDefs()` (save-payload logic factored so **preview === save**) + collapsible `ActivityPreview` pane (`sessionId=null`) in `/teacher/activities/new` + 27 tests. **OPEN:** M1 edit-page parity (needs edit-page element editing), M2 wide-screen side-by-side + "preview as student". Net axiom +6 |
| 1.1.41 | [teacher-sim-resources](teacher-sim-resources.md) | **PRE-PILOT COMPLETE (M0–M3 SHIPPED); M4 post-pilot** | **New 2026-06-22 (M).** **Simulations as reusable activity resources.** Decouples the **artefact** (a vetted MCP-App resource in a catalogue) from the **activity** (the per-instance pedagogy: goal + elements) — so **one sim appears in many activities with different learning goals**. **Completes [1.1.32](teacher-ux-refinement.md)**'s decoupling; answers [1.1.38](activity-elements-palette.md) Q1. **M0–M3 all SHIPPED 2026-06-22 — end-to-end.** M0: `ArtefactMeta` + `backend/artefacts/*.yaml` + `GET /api/artefacts` (`tutorBlock` server-side-only). M1: `artefact_id` (catalogue-validated → `workbench_type=app`) + `/active` resolves it + `GenericArtefactFrame` + chat dispatch (sim = surface, elements below). M2: `compose_teacher_focus()` prefixes the goal with the artefact `tutor_block` in the `{teacher_focus}` injection — **same sim, different goals → different tutoring** (regression-tested); Q1 resolved (reuse `concept-dialogue`). M3: `SimPicker` (browses `live` catalogue) + obsolete 1.1.32 note replaced + `Kastebevægelse` template hosts Boldkast. Additive — 3 legacy sims untouched. `tutorBlock` *content* AR-placeholder (mechanism live; a YAML edit). **OPEN:** M4 legacy migration (post-pilot — don't migrate live pilot sims). Net axiom +11 |
| 1.1.42 | [sim-catalogue-admin](sim-catalogue-admin.md) | **OPEN (design) — POST-PILOT** | **New 2026-06-22 (M).** **Sim catalogue admin — the `tutorBlock` CMS.** Runtime-editable layer over [1.1.41](teacher-sim-resources.md)'s static YAML catalogue: a Firestore override store **merged onto the YAML defaults** (mirrors the custom-personas-in-Firestore v1.2 pattern) so AR/researcher can **edit a sim's `tutorBlock` + metadata, and onboard a deployed sim, without a PR or a deploy**. Researcher-gated ([ADR-016](researcher-role.md)) admin surface + CRUD + audit + resolved-prompt preview (reuse [1.1.27](lesson-author-surface.md)). **Hard boundary: metadata only** — artefact *code* upload stays tier-3 [2.4](../post-pilot/teacher-artefact-authoring.md); `artefactPath` must resolve to a deployed (ADR-013) artefact, so no code path is opened. The middle layer between 1.1.41 (curated YAML) and 2.4 (open marketplace). Net axiom +9 |
| 1.1.43 | [activity-library-sharing](activity-library-sharing.md) | **OPEN (design) — PRE-PILOT (pulled forward 2026-06-24)** | **New 2026-06-23 (M).** **Activities as class-independent, shareable resources.** Promotes the activity from a per-`(teacher, class, activity)` row to a first-class owned **`Activity`** + a class↔activity **assignment** (many-to-many) + **adopt-by-copy** across teachers + a **`draft`/`private`/`published`** visibility state. **Fulfils the cross-teacher-sharing v2 non-goal** both [1.1.19](teacher-activity-authoring.md) and [1.A](../v1.0.0-pilot/implemented/teacher-permission-model.md) deferred, and the **option-(B) decouple** 1.1.19 Q1 flagged for "when branching across classes lands". Generalises 1.1.19 M3 `duplicate`/`source_activity_id`; reuses the 1.1.41 catalogue+adopt pattern + 1.A tagged-ACL. **M0 decouple is independently valuable** — also fixes today's *one concept activity per class* (activity id ≠ skill id). Recommendation: **live-link own→many-classes, copy-on-adopt across teachers** (no shared mutation). Net axiom +7 |
| 1.1.44 | [activity-image-materials](activity-image-materials.md) | **M0–M4 SHIPPED to dev (code) 2026-06-23** (sprint IMG-MAT). Tutor sees the image + student sees it when teacher marks it viewable (dual-audience GET). Pending dev deploy + browser/E2E. | **New 2026-06-23 (M).** **Images the tutor can actually see.** A teacher attaches an **image** (diagram / graph / photographed worksheet) to an activity and the **tutor sees the pixels** in the student conversation — *not* OCR'd text (M's explicit steer: "stored as an artifact the AI can recall and use"). Closes the gap that the "add document" component (`MaterialsSection`) only fed the **text/RAG** pipeline ([1.1.25](curriculum-library.md)). **Reuses the ADK `ArtifactService` as the store** ([`adk/session.py`](../../../../backend/adk/session.py) singleton — shared by upload-handler + runner by design), keyed by an **activity slot** (`aipla/{teacher_uid}/{activity_id}/activity-image:*`). A `before_agent` **loader** copies the image into the student session at session start; a `before_model` **injector** inlines it as an **image** Part — both **twins of the shipped document pipeline** ([`adk/callbacks/document.py`](../../../../backend/adk/callbacks/document.py)), the one new line being an image Part vs a text one. Reuses the proven 1.1.7 `Part.from_bytes` multimodal shape. `MaterialRef` gains a `kind:"curriculum"\|"image"` discriminator. **M0–M3 core (~3.25d); M4 student-display optional/post-pilot.** Gate: JB image-retention posture (teacher-supplied — lower-stakes than student uploads). Net axiom +9 |

**Tally (reconciled against code 2026-06-19; 1.1.38 palette + templates + 1.1.40 M0 + 1.1.41 M0–M3 shipped 2026-06-22; 1.1.43 activity-library-sharing added 2026-06-23, design/post-pilot): 22 SHIPPED · 8 PARTIAL · 13 OPEN (43 rows).**
- **PARTIAL:** 1.1.19 (M0+M1), 1.1.32 (Phase A), 1.1.33 (M0–M2+M4), 1.1.35 (M1–M3), 1.1.37 (send-path), 1.1.38 (M0 registry + M1–M4 elements + starter templates; M5 open), 1.1.40 (M0 live preview; M1–M2 open), 1.1.41 (pre-pilot complete: M0 catalogue + M1 render + M2 prompt-composition + M3 picker; only M4 legacy migration post-pilot).
- **OPEN:** 1.1.3, 1.1.8, 1.1.10, 1.1.17, 1.1.22, 1.1.24, 1.1.27, 1.1.29, 1.1.30, 1.1.31, 1.1.39, 1.1.42 (sim-catalogue-admin, post-pilot).
- **2026-06-19 reconciliation** flipped stale statuses: 1.1.34 + 1.1.36 OPEN→SHIPPED; 1.1.33 understated→M0–M2+M4; 1.1.19 + 1.1.37 OPEN→PARTIAL; 1.1.9 extended with voice STT/TTS cost (`54382bf`). Earlier batches: 1.1.26+1.1.20 (06-09/10); 1.1.12 (06-10/11); 1.1.7+1.1.21+1.1.23 (06-11); 1.1.25 M1–M5 (06-11/12); 1.1.27 re-scoped (06-12); 1.1.5+1.1.9+1.1.4 (06-13). (2026-06-17: 1.1.38 activity-elements-palette + 1.1.39 activity-authoring-assistant — design only, from the 17-June Aswin note; deepen the teacher-authoring axis.) (2026-06-16: 1.1.35 research-audio transcription M1–M3 shipped — Gemini-only STT engine, config-driven model; M4–M6 open. 1.1.36 dual-source narrative + 1.1.37 student-audio-turns — design only.) (15 June added 1.1.29 call-teacher, 1.1.30 mobile-perf, 1.1.31 live-dashboard — all design-only; plus in-place amendments to 1.1.19 RAG-source-selection + 1.1.23 voice-latency-budget.) (1.1.26+1.1.20 2026-06-09/10; 1.1.12 2026-06-10/11; 1.1.7+1.1.21+1.1.23 shipped 2026-06-11; 1.1.25 M1 2026-06-11, M2–M5 + full sprint complete 2026-06-12. 1.1.27 added/re-scoped 2026-06-12. 1.1.5 researcher-role + 1.1.9 cost-dashboard + 1.1.4 session-report shipped 2026-06-13.)

### Beyond the numbered roadmap — also shipped 2026-06-11

These landed this session but aren't numbered rows (they extend shipped work):

| Item | Status | Evidence |
|---|---|---|
| Classroom recording ("Record this class") | **SHIPPED** | `audio-capture-and-tts.md`. `/api/voice/recording` → `gs://{proj}-research-audio` (EU) + Firestore meta + delete-by-group erasure; consent = paper forms, teacher-enabled per class. Sprint `VOICE-IN-REC` M2. Bucket created on dev + NOTES Decision 15 |
| Lesson transcription | **SHIPPED** | Sprint `REC-TRANSCRIPT` (M1–M4): segments transcribed near-live (sync STT) keyed by groupId; student workbench panel (hidden+toggle) + teacher-report section. Commits `ced624f`→`45be532` |
| Default-persona identity | **SHIPPED** | unassigned skills resolve to a default Danish-educator persona (Sofie) for the chat avatar+name (runtime, no seed). Commit `3e3bee3` |
| Per-class persona + style-tie | **SHIPPED** | `Class.persona` + `PUT /api/voice/class/{id}/persona`; `resolve_persona_chain` (activity > class > global default) drives avatar+name+voice+style. `ClassPersonaPanel` in Class settings (surfaces /api/personas errors, not silent); activity builder hides the separate teaching-style control unless "Custom" (persona owns style). Commits `aaf2425`+`f25c839` |
| Seed-reminder CI | **SHIPPED** | `ci.yml` warns on push when a SKILL.md template changed + `make seed` target + CLAUDE.md note. Commit `1de1ed9` |

### Still to do (priority order)

> **POST-MEETING RE-PRIORITISATION (2026-06-11, M).** The biggest teacher/student ask is **multimodal upload — pictures + docs, for BOTH students and teachers**. It moves to the **top**. Voice-**in** is a **requirement**, and `gemini_live` is **deferred** (needs a non-Google/local-Whisper port first), which de-risks it to a small build over the shipped 1.1.11. New top order:
>
> 1. ~~**1.1.7 student multimodal upload** (+ **1.1.21**)~~ — ✅ **SHIPPED 2026-06-11.**
> 2. ~~**1.1.25 teacher curriculum/materials upload**~~ — ✅ **SHIPPED 2026-06-12** (M1–M5: metadata/ACL/browse, RAG ingestion, tutor grounding, builder picker, CLI). Real-world activation needs `bootstrap_rag_corpus.py` + `CURRICULUM_RAG_CORPUS_NAME` on `aipla-dev-2026`.
> 3. ~~**1.1.23 voice-in via `stt_tts_roundtrip`**~~ — ✅ **SHIPPED 2026-06-11** (+ classroom recording + lesson transcription, ahead of the 06-23 date).
>
> **Next, after 1.1.25 finishes:** the unblocked teacher-dashboard wins (1.1.9 cost-dashboard, 1.1.4 session-report — the lesson transcript now feeds it) + the human-gated items teed up (1.1.3 consent, 1.1.8 exit-ticket).

**⚠ Urgent / time-boxed (resolve this week):**

0. **1.1.23 bidirectional voice — TARGET 2026-06-23.** **`stt_tts_roundtrip` is the v1 mode (`gemini_live` DEFERRED — needs a non-Google/local-Whisper port).** Voice-in is a requirement. Now a small build over the shipped 1.1.11 voice stack; the only gate is M's transcript-only GDPR delta. Engineering can start now.

**Human-gated** (tee up first — engineering can't unblock):

1. 1.1.3 consent prompt — JB wording
2. 1.1.7 multimodal upload — JB image-retention posture **+ 1.1.21 guardrail: M (GDPR) on on-device vs Gemini-vision person detection**
3. 1.1.8 exit-ticket — JB/AR question set
4. 1.1.17 engagement signals — JB/AR metric set review
5. 1.1.19 teacher-activity-authoring — M2 quiz format + M4 first workbench type (JB/AR); **M0 is not gated**
6. **1.1.20 tutor-personas — AR sign-off on each preset prompt (concise / rigorous / warm); Socratic is the signed 1.1.1 block**
7. **1.1.24 offline-lab-workbench — JB/AR ground-truth model + 2 real Haka/matematikfysik experiments to model the spec**
8. **1.1.25 curriculum-library — JB/M copyright clearance for the shared A/B/C corpus (aligns with the Strand-C exam-archive thread)**
9. **Part 2 §D (friction/timing analytics) + the 15-June live 5-min class summary ([1.1.31](teacher-analytics-framework.md)) — R1 analytics-framework decision (due before the 2026-06-29 freeze).** R1 now gates **two** things: the post-hoc friction/timing analytics (folds into 2.5) *and* the live dashboard's rolling-summary content (1.1.31 M1+). **Do not instrument friction/timing or the live rubric summary before R1.** The 1.1.31 *shell + raised-hand panel + deterministic signals* are un-gated and can build now.
10. 1.1.27 lesson-author-surface — JB sign-off on what the resolved-prompt preview reveals (verbatim skill template vs summarised). Engineering can build the assembler + trial session unblocked; only the preview's reveal-policy is gated.

**No human dependency** (can start now):

- **1.1.26 teacher-UI-consolidation P1 (design-system primitives)** — frontend-only, ~1.5d. **DECIDED FIRST (M, 2026-06-09): a hard prerequisite, not soft** — "in place first before we add more complexity to it." P1 + the class-detail (P3) + builder (P4) refactors land **before** the config-heavy rows (personas 1.1.20, voice 1.1.23, TTL 1.1.10, budget 1.1.9, quiz/materials 1.1.19 M2–M8) so each new config slots into the pattern, not the old mess (no refactor-twice).
- **1.1.19 teacher-activity-authoring M0** — no-workbench concept activity, end-to-end (~1.5d, lands pre-freeze; the 3-June headline ask). **M0+M1 already shipped (commits 3f65f97 → baeaae5).**
- ~~1.1.5 researcher-role~~ — ✅ **SHIPPED 2026-06-13** (unblocked 1.1.9 researcher/cohort views).
- ~~1.1.9 cost-dashboard~~ — ✅ **SHIPPED 2026-06-13** (teacher BudgetPanel + researcher Cost tab).
- ~~1.1.4 session-report summary-primary~~ — ✅ **SHIPPED 2026-06-13** (AI narrative summary-first + collapsible transcript).
- 1.1.10 teacher-choice-ttl (~0.5–1d)
- 1.1.12 voice-personas — finish Persona model + cards (~2d remaining)
- **1.1.22 end-of-class-notes-summary** (~1d) — *but sequence after 1.1.7 lands* (composes its upload path)

**Sequencing note:** 1.1.24 offline-lab depends on 1.J (Type 5) so stays **post-mid-point**. 1.1.25 curriculum-library was *de-gated 2026-06-09* — on **managed ADK RAG** (not pgvector) it no longer waits on 1.3, so it (and its M7/M8 pull-forwards) can land earlier; the only remaining input is the B/C corpus being parsed + copyright clearance. 1.1.20–1.1.22 are small and interleave with the existing P1s before the freeze; 1.1.23 is the only date-forced item.

### Numbering note (1.1.14 collision)

Commits `18f382e` / `62c9205` registered **voice-pronunciation-config** as 1.1.14, but the ordering table later assigned 1.1.14 to **chat-history-flicker-on-token-refresh**. Resolved here: chat-history-flicker keeps **1.1.14** (matches the table); voice-pronunciation-config becomes **1.1.18**.

### Doc-location hygiene (follow-up — not done in this pass)

Per the Cross-version convention below, these SHIPPED design docs should move to `implemented/` alongside their sprints: `tutor-verbosity-fix`, `proactive-sim-reactive-tutor`, `group-code-school-year-ttl`, `voice-provider-abstraction` (+sprint), `voice-pronunciation-config`, `security-monitoring-pipeline`. Deferred deliberately: each is referenced by 3–8 docs at differing relative depths (plus pre-existing rot from the sprint docs already moved without inbound-link updates), so the relocation is a dedicated link-migration pass, not part of a status fix. Tracked as the next hygiene task.

## What's gated on human input

Tee these up first — engineering can't unblock them:

1. **JB sign-off on consent prompt wording** ([1.1.3](student-consent-prompt.md)) — gates the chat-log opt-in shipping
2. **JB/AR question set for the exit ticket** ([1.1.8](exit-ticket.md)) — gates the modal shipping
3. **AR sign-off on the verbosity-trimmed system prompt** ([1.1.1](implemented/tutor-verbosity-fix.md)) — gates the prompt change merge
4. **JB confirm on image-retention posture** ([1.1.7](student-multimodal-upload.md)) — gates multimodal upload; brief states "images sent to Gemini, not retained in BQ" but needs explicit JB OK

**Added 2026-06-09 (9 June batch):**

5. **M/JB — bidirectional voice: per-mode GDPR + first-enabled mode** ([1.1.23](bidirectional-voice-brief.md)) — **time-boxed to 2026-06-23**; the most urgent gate in the batch. M: per-mode posture (`gemini_live` = full audio review, `stt_tts_roundtrip` = delta); JB: which mode demos first. Not "who owns a build" — it's a config axis. Resolve today.
6. **M (GDPR) — person-detection approach** for the upload guardrail ([1.1.21](student-multimodal-upload.md)) — on-device (no pre-check egress) vs Gemini-vision pre-check
7. **AR — per-preset persona prompts** ([1.1.20](tutor-personas.md)) — `concise` / `rigorous` / `warm` wording (Socratic = the signed 1.1.1 block)
8. **JB/AR — offline-lab ground-truth model + 2 real experiments** ([1.1.24](offline-lab-workbench.md)) — confirms "ranges + relationships, no symbolic-math engine" and seeds the anti-hallucination spec
9. **JB/M — curriculum-corpus copyright clearance** ([1.1.25](curriculum-library.md)) — only `cleared` material enters the shared A/B/C library; same thread as the Strand-C 2010 exam archive
10. **R1 analytics-framework decision** (ICAP+FCI vs CPS+DRA), due before the 2026-06-29 freeze — gates Part 2 §D friction/timing analytics (folds into 2.5) **and** the live 5-min class-summary content in [1.1.31](teacher-analytics-framework.md) (added 15 June). The 1.1.31 shell + raised-hand + deterministic signals are un-gated.

Items with **no human dependency** can start immediately (see *Still to do → No human dependency*).

## Items that can ship in parallel

The dependency graph is shallow — most of these are independent surfaces:

```
Independent (start any time, in any order):
  1.1.1 verbosity         (2h)   ───┐
  1.1.2 sim-reactive      (1d)   ───┤
  1.1.4 summary primary   (1d)   ───┼─► no blockers; parallelisable
  1.1.5 researcher role   (1d)   ───┤
  1.1.6 TTL extension     (2h)   ───┘

Human-gated:
  1.1.3 consent prompt    (1d)    ◄── JB wording approval
  1.1.8 exit ticket       (1d)    ◄── JB/AR question set

Larger / more cross-cutting:
  1.1.7  multimodal upload         (2d)   ◄── ADR-008 ready; JB image-retention confirm
  1.1.9  cost dashboard            (1d)   ◄── 1.2 BQ tables shipped; uses existing OTel
  1.1.11 voice provider abstraction (3.5-4d) ◄── 1.0 TTS shipped; 1.1.2 proactive (auto-read picks up); 1.1.9 spans; 1.G toggle
  1.1.12 voice personas             (3d)   ◄── 1.1.11 shipped; 1.G picker placement; optional 1.1.9 + 1.1.5
  1.1.14 voice pronunciation config (1d)   ◄── 1.1.11 shipped; cc8507f inline list to extract; ships in parallel with 1.1.12
```

## Items the brief explicitly excluded (future)

From the brief's "Not in this brief" section — captured here so they don't get lost:

| Item | Why not now | Where it goes |
|---|---|---|
| Bidirectional voice (student speaks → STT → tutor responds) | Significant UX change; needs a research angle baked in | Separate post-v1.1 sprint; partly overlaps [audio-capture-and-tts.md](../v1.0.0-pilot/audio-capture-and-tts.md) |
| Teacher activity creation from scratch (Parameters tab scope expansion) | Significant feature — beyond bounded knobs | Separate design doc; overlaps [post-pilot/teacher-artefact-parameters.md](../post-pilot/teacher-artefact-parameters.md) |
| Activity branching / marketplace | Needs ≥10 activities to be useful | Separate sprint, post-pilot |
| Oral exam prep skill | New skill brief — DRA map per topic + voice mode | New skill brief in scoping site |
| Student note-taking skill | New skill brief | New skill brief in scoping site |
| Portfolio download | Depends on [1.1.6](implemented/group-code-school-year-ttl.md) school-year TTL shipping first | Small follow-up sprint after 1.1.6 |
| Proactive cooldown event-banking | Tutor references the SEQUENCE of student actions during cooldown ("you tried 30°, 45°, 60° in the last 30s"), not just the latest. v1.1 ships with state-aware-but-not-history-aware reactive turns. Validate need from pilot session reviews first | [cooldown-event-banking.md](cooldown-event-banking.md) — roadmap signal, not committed |

## Total estimate

| Bucket | Engineering days |
|---|---|
| Quick wins (1.1.1, 1.1.6) | ~0.5d |
| Standard (1.1.2, 1.1.3, 1.1.4, 1.1.5, 1.1.8, 1.1.9, 1.1.10) | ~6.5-7d |
| Larger (1.1.7, 1.1.11) | ~5.5-6d |
| Polish (1.1.12, 1.1.14) | ~4d |
| 3-June batch subtotal | **~16.5-17.5d** |
| **9-June Part 1** (1.1.20 personas ~1d, 1.1.21 guardrail+units ~1.25d, 1.1.22 notes-summary ~1d) | ~3.25d |
| **9-June urgent** (1.1.23 `voice_mode` axis ~0.5d + `stt_tts_roundtrip` ~0.5–1.5d; `gemini_live` mode +~3–5d when enabled) | ~1–2d for the date; +3–5d for the live mode |
| **9-June Part 2** (1.1.24 offline-lab ~3-4d *post-mid-point, on 1.J*; 1.1.25 curriculum-library ~3-4d *ADK RAG — un-gated, can land earlier*) | ~6-8d |
| **Breadth pull-forwards** (1.1.19 M7 auto-rubrics ~2d + M8 coverage-map ~1-1.5d) — *consume 1.1.25; high-value-as-unblocked* | ~3-3.5d |
| **Total (incl. 9-June batch)** | **~30-36d** |

The 3-June subtotal comfortably fit the window. The 9-June batch adds **~14-18d**, of which only Part 1 (~3.25d) + the urgent 1.1.23 axis+cheap-mode (~1-2d) target the pre-freeze / pre-pilot window; the Part-2 docs + breadth pull-forwards (~9-11.5d) are **post-mid-point, gated on 1.J + 1.3**, and absorb into 2026-07-06 → 08-14 + pilot iteration. The batch does **not** fit before the freeze in full — Part 1 + 1.1.23 are the realistic pre-freeze slice. **Under breadth-over-depth, the curriculum multiplier (1.1.25) + its pull-forwards (M7/M8) are the highest-leverage post-freeze work** — they make every subsequent probe cheaper.

## Timeline anchors

- **2026-06-03** — Brief delivered (3 June teacher check-in)
- **2026-06-04** — v1.1 design docs landed
- **2026-06-09** — 9 June teacher check-in; batch 1.1.20–1.1.25 design docs landed (this commit)
- **2026-06-15** — 15 June teacher check-in; batch 1.1.29–1.1.31 + the 1.1.19/1.1.23 amendments landed (this commit). Mostly downstream of 9 June; no scope explosion
- **2026-06-16** — **Demo.** UI feedback expected (make-own-class / make-groups / boldkast — all shipped); M processes feedback after. Nothing app-side needed pre-demo. Also: teacher-section AIPLA logo shipped; 1.1.34 [human-tool-use-card-persistence](human-tool-use-card-persistence.md) design-only (restore MCP-app interaction cards on reload — M's observation that reload shows only the tutor's reaction, not the interaction)
- **2026-06-23** — **⚠ Bidirectional-voice target (1.1.23)** — the one date-forced item; ahead of the freeze. Architecture + GDPR + owner must be locked by ~2026-06-11 to hold it
- **2026-06-26** — Mid-point review; quick wins (1.1.1, 1.1.6) shipped, 1.1.19 M0+M1 shipped, ≥1 larger item in flight
- **2026-06-29 → 07-05** — Holiday freeze. R1 analytics decision must land before this (gates Part 2 §D)
- **2026-07-06 → 08-14** — v1.1 build window (post-freeze, pre-pilot). Part-2 docs 1.1.24 (offline-lab, after 1.J) + 1.1.25 (curriculum-library, after 1.3) build here
- **2026-08-14** — Pilot starts; ideally 1.1.1–1.1.6 + 1.1.9 + 1.1.20–1.1.23 live
- **2026-08-14 → 09-15** — Pilot iteration window for remaining items + portfolio-download follow-up

## Sprint status — historical sprint narrative (2026-06-03)

> **Superseded for status-tracking** by [Build status — verified 2026-06-08](#build-status--verified-2026-06-08) above. Retained below for the QUICK-WINS-V11 / PROACTIVE-SIM-REACTIVE sprint detail (test counts, recon findings).

**Shipped (merged to `dev`):**

- ✓ **QUICK-WINS-V11** — 1.1.1 verbosity + 1.1.6 rename-and-archival bundled sprint. Track A: 3 commits (constraint block in 3 SKILL.md files; pytest regression guard; slow-marked LLM smoke). Track B: 3 commits (constant rename, archive-on-expiry + 410 Gone, value-revert to 30d after mid-sprint privacy rethink). Plus M7 archival integration tests (3 cases). Shipped 2026-06-03 at `9d03c61`. Sprint doc moved to [implemented/quick-wins-v1.1-sprint.md](implemented/quick-wins-v1.1-sprint.md). Backend test count went 1637 → 1645 (+8 net).
  - **Mid-sprint rescope**: original 1.1.6 plan to lift platform default 30d→300d was reshaped on privacy grounds. Default stays 30d; the per-code teacher-choice path becomes [1.1.10 teacher-choice-ttl.md](teacher-choice-ttl.md). Both 1.1.6 design doc and v1.1 SEQUENCE updated to reflect this.
- ✓ **PROACTIVE-SIM-REACTIVE** — 1.1.2 sim-event-reactive tutor (Phase B, Path B confirmed). 10 milestones M1–M10 shipped same-day 2026-06-03. Backend test count went 1645 → 1695 (+50 net: 7 from M2 SkillConfig fields, 7 from M3 inject_reactive_guidance, 2 from M4 session counters, 16 from M5 endpoint, 3 from M6 SKILL.md templates, 15 from M7 OTel). Frontend test count went 798 → 827 (+29 net: 12 from sentinel detector, 17 from event-check client). Architecture: backend `/proactive-event-check` is a pure gate decision; frontend kicks off the AG-UI run via `useSkillAgent.sendMessage` with a `[event_reactive:<kind>]` sentinel so the proactive turn rides the established AG-UI protocol. Sentinel suppression in `toSkillMessage` keeps the trigger from rendering as a student bubble.
  - **M1 recon finding**: `useSkillAgent.sendMessage` was confirmed as the FE programmatic trigger API; Path B proceeded as planned.
  - **M7 recon finding**: the `tutor.proactive_kind` OTel attribute mentioned in Phase A's docstring was aspirational and never implemented. M7 created the seam fresh (`backend/adk/proactive_telemetry.py`) covering both Phase A's `[session_start]` and Phase B's `[event_reactive:*]` uniformly.
  - **Sprint doc** to be moved to [implemented/proactive-sim-reactive-tutor-sprint.md](implemented/proactive-sim-reactive-tutor-sprint.md).

**Pending (M4 manual sanity for the verbosity prompt):**

- LOCAL_MODE chat against `led-planck-tutor`: confirm first 5 tutor turns each ≤3 sentences and end with `?`. Counter-test: `"forklar i detaljer"` → next turn allowed to be longer. Same against `kinebot-kinematics-tutor` in English. Non-blocking — M2 + M3 self-tests are the regression bar.

**Filed follow-ups (smaller post-sprint items):**

- [proactive-greet-refactor-to-path-b.md](proactive-greet-refactor-to-path-b.md) — refactor Phase A `/greet` onto the Path B rail Phase B now establishes. ~0.5d. Land after Phase B has been exercised through a few pilot sessions.
- **ProgressChecklist proactive-trigger hook** — checklist toggle → `step_advance` event. Deferred from PROACTIVE-SIM-REACTIVE M8 because it requires threading `onChatMessage` through a mount path that doesn't currently have it. Lower priority than the MCPAppToolCallRouter sim-run path which already covers Boldkast (the primary case). ~0.25d when it lands.
- **Activate Dependabot auto-close + fix the weekly alert-count query** (LOWER PRIORITY, deferred 2026-06-15) — both the weekly cron's alert-count line and the new auto-reconcile step (`scripts/reconcile-dependabot-alerts.sh`, shipped + verified) are blocked on one thing: the Actions `GITHUB_TOKEN` gets HTTP 403 on the Dependabot alerts API. Fix = add a `DEPENDABOT_TOKEN` fine-grained PAT repo secret (Dependabot alerts: read+write); the workflow already prefers it and no-ops gracefully without it. Not urgent — the CI gate (Layer 1) is the real blocker and is unaffected; manual `/aipla-security-checkup` covers cleanup meanwhile. ~10 min when someone decides on PAT/secret policy. Full context in [implemented/security-monitoring-pipeline.md](implemented/security-monitoring-pipeline.md).

- [teacher-coworking-copilot.md](teacher-coworking-copilot.md) (DESIGN 2026-06-27, **P1**) — unify every teacher AI helper (manage-class, analytics, activity-authoring) onto ONE shared floating co-pilot with the propose → Apply/Edit/Dismiss model the activity co-pilot already ships: on the page the human works on, AI changes land alongside the human's own edits, nothing takes effect without Apply (co-working partnership / earned trust). Parts: **1** extract shared co-pilot shell (`components/teacher/copilot/`), **2** class-management co-pilot (propose-only writes + Apply→REST→list refetch), **3** analytics panel (read-only), **4a** resume across visits, **4b** scoped history tier. Build order 1→2→3→4. **Parts 1–4a SHIPPED 2026-06-27** (shared shell `063b9d5`; class co-pilot `0b4cf26` deployed+reseeded; analytics co-pilot `80554d7`; resume `4887ff3`) — plus two enabling fixes: seed-skillMetadata (`c4098c0`) and AG-UI token-rotation (`d2c59b3`). Remaining → the two child docs below.
- [teacher-chat-history-tier.md](teacher-chat-history-tier.md) (DESIGN 2026-06-27, **P2**, = Part 4b) — browse + reopen PAST co-pilot chats grouped by class. Backend: `classId`/`kind` tag on `ChatSessionIndex` at session creation + `GET /api/teacher/chats`; frontend: history affordance in the shell + class-level "Conversations" view (reopen via shipped resume). Clean-session sprint.
- [activity-copilot-shared-shell-migration.md](activity-copilot-shared-shell-migration.md) (DESIGN 2026-06-27, **P2**) — re-express `_AuthoringCopilot.tsx` on the shared shell (delete ~250 lines of duplicate chrome; no behaviour change). Recon done: small shell config knobs + 7 element previews → `ProposalDescriptor.body` + test adaptations. ⚠ the other agent's 15-test file — coordinate. Clean-session sprint.

## Shipped — mid-point → pre-freeze week (2026-06-22 → 06-28)

A heavy pre-freeze push (mid-point review 26 June; M+JB freeze week 27 begins 29 June). The 2026-06-08 Build-status table below is now stale; this block is the authoritative "what landed on dev this week", by workstream. (~195 commits.)

- **Teacher co-working co-pilot** (this batch's headline; [teacher-coworking-copilot.md](teacher-coworking-copilot.md)) — shared floating co-pilot shell (`components/teacher/copilot/`) + **class-management co-pilot** (propose/Apply on `/teacher/classes`) + **scoped read-only analytics co-pilot** on the class detail page + **cross-visit resume**. Plus enablers: manage-class promoted to an active tool hub (`agent_tools` AgentTool delegation), seed-skillMetadata propagation fix, AG-UI token-rotation fix, markdown-render + Applied-badge fixes. **Parts 1–4a shipped; Part 4b (history tier) + the `_AuthoringCopilot` shell-migration are designed, not built.**
- **Activity-authoring co-pilot** (COPILOT-1 + COPILOT-2; [activity-authoring-assistant.md](activity-authoring-assistant.md)) — the AI composes an activity from a teacher's description: `set_lesson_prompt`, `add_element` (checklist/table/chart/calculator/note/solution/document), `set_artefact` (propose a sim), floating panel, mounted on builder + `/new`. Dark-flagged → enabled on dev.
- **ALS-SHARE — cross-teacher activity sharing** ([implemented/activity-library-sharing](implemented/)) — duplicate/branch, publish + shared catalogue + adopt, researcher CRUD, provenance/history panel, unified status lifecycle + visibility setter, separate researcher Research surface. M0–M3 done; **M4 attribution deferred (optional).**
- **EXT-MCP — sims as portable MCP Apps** ([external-host-mcp-apps.md](external-host-mcp-apps.md)) — public `/api/mcp` route, sims exposed as `ui://` MCP Apps with CSP, `content`+`structuredContent` emission for external-host models. Phase 1+2 shipped.
- **Researcher analytics (1.1.51)** — cross-class drill-downs, overview, transcripts; the researcher Research surface; friendly owner-name labels across class/activity views.
- **Workbench / activity polish** — element palette (table/chart/calculator/note, 06-22), "shared with the AI" trust-card audit across elements, single sim render path (USR-1, legacy slug path deleted), first-party workspace elements reach the tutor on concept activities, lazy ADK session for early workbench events, class list reads the `activityIds` model, draft-status lifecycle cleanup, activity preview modal as a real Radix dialog.
- **Onboarding** — clean-slate dev wipe + auto-seed a demo for new teachers.
- **Thinking budget** — env-level layer ([thinking-budget-configuration.md](thinking-budget-configuration.md)); per-skill/per-turn/persona layers still Planned.
- **Ops** — `make seed-demo-codes` (stop `aipla-demo-1` lapsing), 2 prod frontend dep patches, greet-spinner race fix.
- **RICH-DOC — rich-document workbench** ([rich-document-workbench.md](rich-document-workbench.md), started 06-24) — M0–M3 shipped: rich parsed-doc rendering (ChatMarkdown, not a `<pre>` dump), Documents tab + activity-driven workbench shell, in-place document viewer (react-pdf page-nav/zoom/fullscreen + ACL-gated `GET /api/documents/{id}/raw`), student upload + active-file → tutor `document_ids`. **M4 (TipTap rich-text solution editor) was superseded the next day by SUBMIT-1** ([student-submission-surfaces.md](student-submission-surfaces.md)): the solution element became image-based (photo + freehand whiteboard) and TipTap was removed (`ff7c889`). Nothing outstanding.

## Cross-version updates

When these ship, mark them in this file's `Sprint status` section (mirroring the v1.0 pattern) and move docs into a sibling `implemented/` directory.
