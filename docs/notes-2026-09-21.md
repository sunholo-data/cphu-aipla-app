# notes 21st September 2026

**Raw meeting capture, unedited, for triage.** Same convention as
[notes-2026-09-16.md](notes-2026-09-16.md) — grouped under headings for
legibility, wording left as spoken. Anything guessed at rather than heard
clearly goes in [Terms I inferred](#terms-i-inferred) rather than being
silently resolved.

Attendees: TBD. Named in this meeting: Jesper (JB), Aswin, Tabita, Atharva
(AD) — presence not confirmed for any beyond M.

## The meeting in one page

**Week recap given at the top of the meeting** — see
[the weekly summary](#weekly-summary-2026-09-15-to-09-21) below for the full
shipped-work rundown (teacher-nav consolidation, researcher onboarding,
tutor-fidelity reporting, the workbench-upload fix, wave sims, the how-to
guides moving off Quarto).

**Jesper is using AIPLA himself to build teaching activities**, and has been
adding other teachers to try it. General signal: it has "worked ok" so far;
**younger teachers (~50) are more keen on new systems** than older colleagues
— read as a rough adoption-curve observation, not a specific complaint.

**Aswin built activities and gave feedback via Teams, including a document
upload that would not work.** This is the same report already tracked in
[frontend-client-logs.md](design/aipla/v1.1.0-feedback/frontend-client-logs.md)
(1.1.117/1.1.119) — and, checked in this session, it has since been **root
caused and fixed**: the upload was silently succeeding but storing an empty
`skillId` (a form field misread as a query parameter on the backend), so the
file landed in Firestore but never appeared in the workbench list. Fixed
2026-09-16 ([1.1.121](design/aipla/v1.1.0-feedback/SEQUENCE.md)), shipped to
prod today in the v0.1.60 promotion. The "ROOT CAUSE UNKNOWN" language still
sitting in the SEQUENCE.md entry is now stale and should be corrected to
point at the fix.

**Testing tomorrow, 2026-09-22**: a 1st-year class, to support **Tabita's
gameboard activity** on physics concepts. Confirmed the upload fix is live in
whichever environment they test in (prod is on v0.1.60 as of today, which
includes 1.1.121).

**Other teachers want to use the platform for maths, not just physics.**
Unscoped — no activity or teacher named yet.

**Jesper is going to Birkerød in about a month** (~mid/late October) to look
at activities that transfer from folkeskole to gymnasium (high school). Noted
as harder than physics-in-context work because the material gets **less
concrete, more abstract** at that transition.

**A generated simulation's graph was physics-inaccurate.** No specifics yet on
which sim or which graph, or whether it was a wrong equation, wrong constants,
or a canned animation not actually driven by the physics. **Parked — will
look further once specifics are in hand**, possibly feeding into the sim
[authoring prompt](../.claude/skills/mcp-app-artefact/resources/authoring-prompt.md),
which today has no explicit "the physics must be correct" requirement (only a
self-test on arithmetic, plus an explicit disclaimer that a human subject
reviewer is still required).

**Jesper wants to go out again to speak to students in the next few months**,
and separately, to **gather feedback from teachers on what the platform can do
for them.** No date yet — read as the same site-visit push as the item above,
not confirmed.

**Jesper is teaching experienced high school teachers, testing AIPLA with them
next week** (~2026-09-28).

**Atharva is going to fork the repo and start working on it next week-ish**
(~2026-09-28) — confirmed same person as "AD" in `CLAUDE.md`'s fork context, a
colleague starting ~2026-10-01 with ~6 months' overlap, onboarding by pairing.
`sunholo-data/cphu-aipla-app` is confirmed **public** (`gh repo view`), so no
access grant is needed for a fork — a plain GitHub fork-and-PR workflow works
today with zero setup. Worth deciding *now*, ahead of AD starting, whether he
should instead become a `sunholo-data` org member with direct push (matching
how the bot account works, no PRs for `dev`), since a fork-based workflow
conflicts with the repo's own git policy (commit straight to `dev`, no PRs).

**Separately: Jesper is preparing a legal offering — a Data Management
Agreement — for the Google decision.** Legal/contract track, no repo
visibility, noted for awareness only.

## Weekly summary (2026-09-15 to 09-21)

Shipped this week, promoted to prod as v0.1.60:

- Teacher navigation consolidated: one "Ask AIPLA" header button as the single
  entry to every co-pilot; Settings/Approaches moved to an account menu;
  parallel research tree retired for a library scope toggle + Insights tab.
- Researcher-led class onboarding: onboarding-stage tracking, a "who's stuck,
  what's next" view, and a context-aware help co-pilot.
- Researchers can now edit (not delete) another teacher's class, attributed.
- Session reports now judge whether the tutor's chosen teaching approach was
  actually followed, not just assigned (1.1.107).
- Workbench upload bug fixed (see above) — root cause of Aswin's report.
- Other fixes: `.txt` uploads no longer stuck in pending-parse forever;
  progress calls no longer 404/403 for missing skill id; uploads capped at
  20 MB; unified upload routing.
- Two new wave simulations (wave speed, interference); the sim authoring
  prompt published publicly for physics staff to draft sims themselves.
- The how-to guides fully moved from Quarto documents to in-app pages.
- Client-side error reporting (crash visibility) landed.

## Decisions

- None taken this meeting — mostly status/scheduling. The upload root-cause
  finding was *confirmed*, not decided (it was already fixed in the repo
  before this meeting).

## Actions

| # | Action | Owner | By when |
|---|---|---|---|
| 1 | Correct the stale "ROOT CAUSE UNKNOWN" language in the [1.1.119 SEQUENCE.md entry](design/aipla/v1.1.0-feedback/frontend-client-logs.md) to point at the 1.1.121 fix | | |
| 2 | Confirm tomorrow's Tabita gameboard test (2026-09-22, 1st-year class) goes cleanly — watch for any upload/workbench issues given the fix only just reached prod | | 2026-09-22 |
| 3 | Get specifics on the physics-inaccurate sim graph (which sim, which graph, wrong equation vs. wrong constants vs. canned animation) before touching the authoring prompt | | |
| 4 | Decide AD's workflow: fork-and-PR (no setup needed, repo is public) vs. `sunholo-data` org membership with direct push to `dev` (matches existing git policy, no PRs) | | ~2026-09-28 |
| 5 | Prepare anything needed for next week's experienced-teacher testing session (Jesper-led) | | ~2026-09-28 |
| 6 | Scope the maths-expansion ask against the extension's stated capacity (~2 substantial workstreams) before committing | | |
| 7 | Track Jesper's Birkerød visit outcome (folkeskole→gymnasium transfer activities) — likely feeds a scope conversation on abstraction-heavy content | | ~mid/late Oct 2026 |

## Dates

- **2026-09-22** — Tabita gameboard test, 1st-year class
- **~2026-09-28** — experienced high-school teachers test AIPLA (Jesper); Atharav forks repo, starts working
- **~mid/late October 2026** — Jesper → Birkerød

## Terms I inferred

- **"Birkerole"** — read as **Birkerød**, a town/school in Denmark. Not
  confirmed spelling.
- **"folkeskool"** — read as **folkeskole** (Danish primary/lower-secondary
  school), contrasted with gymnasium (Danish high school).
- **"Atharva"** — confirmed same person as "AD" in `CLAUDE.md`'s fork context,
  and the same "Atharva" referenced (not present) in the 2026-09-09 meeting
  notes.
- **"jesper going to birkrole ... acitvies that transfer form folkeskool to
  high school"** — read as: activities designed for the folkeskole→gymnasium
  transition, which get pedagogically harder because the content is less
  concrete/more abstract at that level. Not confirmed against Jesper directly.

## Triage status

**Not triaged against design docs.** Nothing raised this meeting is a new
build ask — it's scheduling, adoption signal, and one already-resolved defect
(the upload bug, confirmed fixed in-session). The one open thread with real
design weight is the physics-inaccurate sim graph, parked pending specifics
(see Action 3); revisit with a proper triage once that detail exists.
